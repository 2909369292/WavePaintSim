# 2026-09-13 WavePaint 工作日志

## 本轮目标
1. 汇总并回答用户两问：**当前后端能力到底支持到什么程度**、**要模仿 Verdi/Word 式「波形窗口 + 代码窗口 + 代码树窗口 + 其他窗口」自由拖拽排列，UI 要大改，该怎么排期**。
2. 把「目标形态 = 可停靠（dockable）多面板」从口头需求落成**可执行的分期规划**，写进记忆供下一位 AI 直接接手。

## 做了什么
- 只读盘点后端（`WavePaintLauncher.cs` 62,598 B）与前端（`index.html` 1038 行 + `js/sim/*`）现状，逐条取证到行号，未改一行产品代码。
- 在 `memory/08-ROADMAP.md` **新增 §6**（约 200 行）：§6.1 后端能力表（带行号证据）/ §6.2 后端缺口 G1~G8 / §6.3 前端面板现状 / §6.4 与 Verdi 的差距表 / §6.5 选型对比 / §6.6 目标架构 / §6.7 契约面与 e2e 影响面 / §6.8 分期（#94 D0~D4、#95 E1~E5、#96 W1~W3）/ §6.9 验收与回滚 / §6.10 四条待拍板 / §6.11 与远期项（#84/#77/#78）的关系。
- 在 `memory/03-REQUIREMENTS.md` 新增**表 K**（#94 / #95 / #96 三条新需求，含完整备注）。
- 在 `memory/04-PROGRESS.md` 更新 §1 快照（日期 → 2026-09-13、新增第二十二轮行、重写「下一步主线」）、§2 时间线补一行、**新增 §4.20**（10 小节全记录）。
- 在 `memory/07-DECISIONS.md` 新增 **D22**：dockable 目标形态 + 10 条已定裁决 + 四条待拍板表。
- 在 `memory/09-HANDOFF.md` 更新 §1 三行（最近完成 / 最近完成文档 / 下一步）并新增第二十二轮段（旧段改标「上一轮」）。
- 本文件（`memory/logs/2026-09-13.md`）+ `05-LOGS.md` 索引行 + `INDEX.md` 页脚同步更新。

## 关键结论
- **后端闭环已通，但范式单一**：`/api/sim`(:357) → `RunSimulation`(:443) → 拼包落 `%TEMP%\ivl_work_<guid>` → `EnsureIvlReady` 自愈（内嵌 `ivl.zip`）→ `BuildCompileBatches`（primary = `-g2012 -s tb -o sim.vvp <files>`，含 `.sv/.svh` 追加 `fallback_v`）→ `vvp sim.vvp` → 读 `wave_out.vcd` **整文件文本**回传(:529~:535)。常量化门槛：`ProcessTimeoutMs = 30000`(:17)、`MaxBodyBytes = 8 MiB`(:18)、`MaxSnapshots = 20`(:19)、`PreferredPort = 17817`(:28)、`PingTag = "WAVEPAINT-SERVICE"`(:31)。
- **最大缺口 G1 = TB 只读**：`#tb-source` 是 `readonly` textarea（`index.html` L926），TB 只能由 `buildAutoTestbench` 生成（`ui-bridge.js:1305` / `engine.js:758`）→ 现在只是「画布即激励」的玩具仿真器，不是通用仿真器；`#95 E1`（TB 可编辑）+ `E2`（日志/进度/取消）是「通用仿真器」的最小充分集，建议与 #94 并行优先。
- **G6 已实现但没接线**：`/api/snapshot`(:382 → `SaveSnapshot`:580) 后端存档/保留 20 份已完成，前端全仓 `rg api/snapshot` 只命中 README + launcher → 归入 `#96 W2`。
- **UI 现状 ≠ 目标形态**：现在只有右侧单栏 4 张可折叠卡（第二十一轮落地），要 Verdi 式四区（LEFT 树 / CENTER 主视图 / RIGHT 源码 / BOTTOM 控制台）+ Word 式自由拖拽，差距 4 项都是「大」。
- **选型建议 = 自研轻量 dock 引擎**（`js/sim/dock/*.js`：`PanelRegistry` + `LayoutTree` + `DockDnD`），面板 = 已存在 DOM 节点只换父容器 → 24 个契约 id 天然不变；备选 Lumino / golden-layout；否决 React 系与 iframe 多窗（会重踩 #82 多窗口抢端口老坑）。仓库有 esbuild 先例（`tools/cm6-entry.js` → `lib/codemirror.bundle.js`），故「引第三方库」是性价比问题、不是技术不可行问题。
- **#94 的关键闸门是 D0**：`PanelRegistry` + 宿主容器抽象必须做到**像素级零视觉变化**，回滚 = 1 个 commit。
- **08 §2.6 的三项剩余直接并入 #94 D1**；#84/#77/#78 仍为远期。

## 验证结果
- `node tools/regression.mjs`：**79/79 通过**（本会话唯一复跑项；其余 e2e 结果沿用上一轮实测值，用户已明确「不必重复核验已确认的事」）。
- 上一轮基线（已在记忆中记录，本轮未重跑）：e2e-ui 86/86、e2e-rtl 61/61、e2e-sim 0 失败、probe-param 全过、exe-smoke 通过、`verify-recovery2` 17/17。
- **本轮零产品代码改动** → 按 `02-WORKFLOW` **C1 未触发，未重建 exe**（exe 仍为 22,012,416 B / 2026-09-11 00:15:01 / `v0.4.0 build 2026-09-11 00:15:01 69b84d0`）。

## 遗留问题
- **四条待拍板（08 §6.10 / 07 D22）**，AI 建议已给，等用户裁决后才能开 #94：① 是否解除「侧栏保持右侧」（建议**解除**，否则做不到 Verdi 式左右分栏）；② 是否解除「层次树暂不左置」（建议**解除**，否则「代码树窗口独立成区」不成立）；③ 浮出 = **页内浮动面板**（建议）vs 真独立窗口；④ 布局是否写进 `.wp`（建议**不写**，只做 sessionStorage + localStorage）。
- 四项用户点名但**本轮未做**（用户已明确「目前不着急做」仍需留痕）：查看波形（#84，远期）、添加信号、打开 top 时自动查找所有例化模块源码（可并入 #94 D1 后的源码区能力）、以及**复数/子复数上下箭头 ±1**、**步长变化时 clock 自动填充**、**编辑模式点 bus 信号误入框选** 等既有 bug（均属交互层，排在 #94 之后）。
- `.e2e-tmp/fix-timeline.mjs`、`.e2e-tmp/fix-handoff.mjs` 为一次性脚本（目录已 gitignore），本轮结束即删。

## 交付提醒
- 本轮**不需要重启应用、不需要重建 exe**（未动产品代码）。
- 下一位接手的 AI：先读 `09-HANDOFF.md` §3 第二十二轮段 → 再读 `08-ROADMAP.md` §6 → 等用户对 §6.10 四条拍板 → 再从 **#94 D0** 开工。

---

# 第二十三轮（同日追加，2026-09-13）

## 本轮目标
1. 承接第二十二轮 `08 §6.10` 的**四条待拍板**，拿到用户裁决。
2. 按用户新指令交付**「不连后端、无功能的假界面」**（零后端纯前端 UI 原型），
   给用户 review **形态**，通过后再做后端接线。

## 用户拍板（四条全部通过）与两条附加指令

| # | 条目 | 结论 |
|---|------|------|
| 1 | 面板形态 | **页内浮动面板**（**不做**真独立 OS 窗口） |
| 2 | 「侧栏保持右侧」 | **解除** |
| 3 | 「层次树暂不左置」 | **解除** |
| 4 | 布局写进 `.wp` | **暂不写**（`.wp` 保持纯激励语义） |

附加：⑤ **G1~G8 全部暂不实现**（#95 整线后延）；⑥ **布局/预设/拖拽手感可先不做**，本阶段只交付「能看」的原型。
→ 实施路径登记为 **D-1（原型评审闸门）**：**D-1 通过 → 才做 D0/D1 接线**。

## 做了什么
- **新增 `prototype/`（3 文件，纯静态，零后端）**：
  `ui-mockup.html`（9,625 B：横幅 + 菜单栏含**布局/面板/窗口** + 工具栏复用 `../img/*.svg` + `#workbench`
  + `#mk-drop`/`#mk-caret` + `#mk-floats` + 三格状态栏 + 交互说明浮层）、
  `ui-mockup.css`（19,279 B：主题变量**逐条抄自** `css/wavepaint.e7b903ef.css` 的 `:root`，类名统一 `mk-` 前缀零冲突）、
  `ui-mockup.js`（45,656 B ≈960 行：**完整轻量停靠引擎原型**）。
- **引擎能力**：布局树 `split{kind,dir,sizes[],children[]}` / `tabs{kind,id,panels[],active}`（与 08 §6.6 目标架构同构）；
  8 面板 `wave/source/rtl/vcd/tb/console/files/props`（波形面板含**真实绘制**的 7 行信号 + 总线标签）；
  3 预设 `presetSim`/`presetEdit`/`presetReview`；`hitTest()` → `tab`/`split`/`edge`（中心合并 Tab / 四边同区切分 / 最外环新建区）；
  拖拽停靠 ghost + 蓝色落点框 + 插入光标 caret；分隔条像素↔比例双向换算；Tab 切换 / 中键或双击浮出 / ✕ 关闭；
  浮动窗拖动 + 右下角 resize + ▣ 最大化 + ⤓ 收回；`Ctrl+Alt+1|2|3` 切预设、`Ctrl+Alt+0` 重置；
  `localStorage['wavepaint.mock.layout.v1']` 400 ms 防抖（**明确不写 `.wp`**）；`window.__mock` 探针 13 个方法。
- **把探针转正**：`.e2e-tmp/mock-probe.mjs` → **`tools/mock-probe.mjs`**（与 `tools/e2e-ui.mjs` 同级，便于下一位 AI 复跑）。
- **记忆同步**：`04 §4.21`（新增全记录）+ `04 §1` 快照（新「最新一轮」行 + 「下一步主线」重写 + 上轮行改标「上一轮」）
  + `04 §2` 时间线补一行 + `03` 表 K（#94 🟡 进入 D-1 / #95 ⏸️ 暂缓 / #96 W3 已拍板不做）+ `03` 表 K 表头补拍板摘要
  + `05-LOGS.md` 索引 + 本日志 + `07 D22`（标题与拍板结论表）+ `08 §6.10`（四条结论 + 新增 §6.10.1 保留追溯）
  + `08 §6.8` 分期表新增 **D-1 行**并把「D0 是关键闸门」改为**两道闸门** + `09-HANDOFF` §1/§3 + `INDEX.md` 页脚 + `01-PROJECT` 目录说明。

## 关键结论
- **主题变量必须抄真机**：原型要「像」，最省事的做法是把 `css/wavepaint.e7b903ef.css` 的 `:root` 变量整段复制，
  而不是自己配色 —— 否则评审时用户会纠结「颜色不对」，淹没对**形态**的判断。
- **类名统一 `mk-` 前缀是硬要求**：原型与真前端**共用同一份 `img/`**、将来还要把结构搬进 `js/sim/dock/*`，
  命名一旦撞车（`.panel`/`.tab`/`.split` 等）就会在接线阶段变成隐性地雷。
- **必须经 `http://` 打开**：`file://` 下 `../img/*.svg` 受同源策略限制，工具栏图标会全丢，看起来像「原型坏了」。
- **真实鼠标拖拽才能验出落点问题**：探针 B 组用 CDP `Input.dispatchMouseEvent` 真拖（非 JS 直接调 API），
  才暴露出「浮窗拖动无落点提示」「`pointerup` 无坐标」两个缺陷 —— 纯 API 调用永远测不到。
- **布局体检（F 组）比功能断言更能发现「丑」**：无横向溢出 / 三段不重叠 / 工作区高度 875 of 1000 /
  四窗格尺寸 / **无裁切文本** / 图片全加载 / 浅色主题生效 —— 这类断言是 UI 原型的「回归网」。

## 验证结果
- **`node tools/mock-probe.mjs` → 34/34 全通过（退出码 0）**。真实 Edge headless + CDP 9532 + `node tools/dev-server.mjs 8951`；
  写法照抄 `tools/e2e-ui.mjs`（`--disable-component-update`、`TEMP/TMP/TMPDIR` → `.e2e-tmp/system-tmp`、
  唯一 `--user-data-dir`、`Emulation.setDeviceMetricsOverride 1680x1000`）。
  断言分组：A 结构 M0~M10 / **B 真实鼠标拖拽** B0~B3 / C 浮出+拖动+收回 / D 预设+隐藏恢复+localStorage / E 无 JS 异常+无资源加载失败 / F 布局体检 F1~F10。
- 截图产物 9 张（`.e2e-tmp/`，已 gitignore）：`mock-1-sim` / `mock-2-drag` / `mock-3-merged` / `mock-4-floats` /
  `mock-5-floats-dragged` / `mock-6-sim-clean` / `mock-7-edit-preset` / `mock-8-review-preset` / `mock-9-help`。
- 实测布局：窗口 1680×1000；`#chrome` y=24 h=75；工具栏底=98；工作区 y=99 h=875（99→974）；状态栏 974→1000；
  四组 = `rtl+vcd` 314×633 / `wave` 910×633 / `source+tb` 430×633 / `console` 1668×223。
- **C1 未触发、未重建 exe**：未改 `index.html` / `js/**` / `css/**` / `img/**` / `lib/**` / `WavePaintLauncher.cs` / `build.ps1`；
  `tools/gen-resources.mjs` 只扫 `index.html` + `css/js/img/lib` + `ivl.zip`，故 **`prototype/` 与 `tools/` 均不进 `resources.txt`、不参与 exe 内嵌**。
  exe 基线仍为 22,012,416 B / 2026-09-11 00:15:01 / `v0.4.0 build 2026-09-11 00:15:01 69b84d0`。

## 本轮原型内修掉的 4 个真缺陷
1. `floatPanel(id, opts)` 忽略 `opts.x/opts.y` → `presetReview` 预置浮窗坐标丢失。**已修**：读取 opts。
2. `applyPreset(name, hard)` 的 `hard` 分支直接 `return presetSim()`、忽略传入预设名 → `__mock.reset()` 与 `Ctrl+Alt+0` 语义混乱。**已修**：统一走 `PRESETS[name]`。
3. 浮动窗拖动时**不显示落点提示**（与停靠拖拽体验不一致）。**已修**：`move` 补 `showIndicator(hitTest(...))`、`up` 补 `hideIndicator()`。
4. 浮动窗拖动松手时 `pointerup` 可能无坐标 → 落点不更新。**已修**：`if (typeof e.clientX === 'number') move(e)`。

## 遗留问题
- **D-1 闸门未通过**：等用户 review 原型形态（可提修改 → 我改原型这个零风险沙盘）。
- 用户若认可，**下一步 = D0**（面板注册表 + 宿主容器抽象，**像素级零视觉变化**，回滚 = 1 commit），
  然后 **D1** 停靠引擎（把原型的 `split`/`tabs` 模型 + `hitTest` + 落点框搬进 `js/sim/dock/*.js`）。
- **#95（G1~G8）已被用户明确暂不实现** → 恢复前须重新确认意图（方案完整保留在 08 §6.2，勿删）。
- 用户点名的既有交互 bug 仍在队列（排在 #94 之后）：编辑模式点 bus 信号误入框选 / 信号名标位宽 /
  复数子复数上下箭头 ±1 / 步长变化时 clock 自动填充 / 编辑框选后点其他地方自动提交。

## 交付提醒
- 给用户看原型：`node tools/dev-server.mjs 8951` → 浏览器打开 **`http://127.0.0.1:8951/prototype/ui-mockup.html`**
  （**必须 http，`file://` 下 `../img/*.svg` 受限**）。
- 可试交互：拖动面板四向停靠 / 拖到中心合并 Tab / 拖到最外环新建区 / 中键或双击浮出 / 浮窗 ▣ 最大化、⤓ 收回 /
  分隔条调比例 / `Ctrl+Alt+1|2|3` 切三套预设 / `Ctrl+Alt+0` 重置 / 右键看帮助浮层。
- 强调：**纯 mock、假数据、零后端、未动 `index.html`/`js/`、未重建 exe**。

---

## 第二十四轮（2026-09-13）：控件与工具栏规范化 U 线 —— 方案定稿 + 审计工具转正 + 原型 U0 重排

### 用户输入
> 「UI 排布在大体上基本正确，**小的按钮上的排布以及控制栏的排布还需要斟酌，现在似乎有一些混乱**，
> 大体上可以按照此方案进行改动，**请给出之后的工作方案**」

### 判定
- **形态层通过**：第二十三轮交付的四区 dock / 页内浮窗 / 多 Tab / 3 预设**不再改动**（归 `08 §6` D 线）。
- **本轮只做控件层**：按钮尺寸 / 间距 / 分组 / 主次 / 分层 / 溢出，编号 **U0~U4**。
- 用户要的「之后的工作方案」→ 落盘为 `08 §7.8` 总排期表 + `§7.9` 四条待拍板。

### 做了什么
1. **`08-ROADMAP.md §7`（新增，约 130 行，本轮主要产物）**：
   §7.1 基线实测表（真机 / 原型 × 1920 / 1680 / 1440 / 1280）；
   §7.2 问题清单 **P-UI-01~09**（含 **P-UI-02 = 真机 `#toolbar` 1440 溢 32px / 1280 溢 192px 的真 bug**）；
   §7.3 铁律 **R1~R9**；§7.4 目标分层 **L0~L4** + 主方案 **D-UI-A**（21 个图标按钮中 14 个波形类控件下沉 →
   全局带 34→14 个顶层元素）+ 备选 D-UI-B；§7.5 刻度规范（6 个 CSS 变量）；§7.6 `⋯` 溢出与降级；
   §7.7 分期 U0~U4；§7.8 **之后的工作方案（与 D 线合并总排期）**；§7.9 四条待拍板；§7.10 红线。
2. **`tools/ui-audit.mjs` 转正入库**（第二十轮的临时脚本 `.e2e-tmp/ui-audit.mjs` 正式化）：
   真实 Edge headless + CDP，`--mock` / `--real` / `--all` / `--check` / `--widths=`；
   `violationsOf()` 与 R3 / R5 / R6 / R8 / R9 一一对应；输出 JSON，`--check` 时违规非零则退出码非零。
3. **原型 U0 重排**（`prototype/ui-mockup.{html,css,js}`）：`#toolbar` 重写为 L1（11 项 + `⋯`）；
   `panelWave()` 的 L2 五组分带 + `data-ovp` 优先级；`panelSource()` = L3（删重复的「运行仿真」）；
   其余面板带统一 `.mk-tbg` + `⋯`；`:root` 加刻度变量；删 `.mk-hintbar`（R9）；状态栏三段化 + `#st-ctx`；
   新增 §5.5 控件层 = `setupBands()` + `setupFlyouts()` + `setupCtlFeedback()`。
   详见 **04 §4.22 ⑤**（HTML / CSS / JS 三层改动清单）。

### 实测结果（全部实跑）
| 目标 | 命令 | 结果 |
|------|------|------|
| 原型回归 | `node tools/mock-probe.mjs` | **34/34**（失败 0） |
| 原型控件审计 | `node tools/ui-audit.mjs --mock` | 1680 **违规 0** / 1280 **违规 0**（改动前 3 / 6） |
| 真机控件审计（**未修**） | `node tools/ui-audit.mjs --real` | 1920 违规 1 / 1680 违规 1 / 1440 违规 3（溢 32px）/ 1280 违规 3（溢 192px） |

截图产物（`.e2e-tmp/`，已 gitignore）：`ctl-{1920,1680,1440,1280}-toolbar.png`、
`ctl-{1920,1680,1440,1280}-waveband.png`、`ctl-full-1680.png`、`ctl-1280-ovf-open.png`。
1280 实测：L2 的 **T5 对象组**（添加信号 / 选择对象 / 属性）被收进 `⋯`，菜单可正常展开。

### 本轮修掉的自身缺陷
- `setupBands()` 把被 `⋯` 收起的组还原回带内时**顺序错乱**（原实现用 `insertBefore` 依赖兄弟节点位置）→
  改为**无条件按 `order` 重新 `appendChild`**，保证还原后 DOM 顺序与原始一致（`window.__mock.setupBands` 可复现）。

### C1 / exe
- **C1 未触发**：只改 `prototype/**` + 新增 `tools/**`，两者都不进 `resources.txt`
  （`tools/gen-resources.mjs` 只扫 `index.html` + `css/js/img/lib` + `ivl.zip`）→ **未重建 exe**。
  exe 基线仍为 22,012,416 B / 2026-09-11 00:15:01 / `v0.4.0 build 2026-09-11 00:15:01 69b84d0`。

### 遗留问题
- **U-1 闸门未过**：等用户看截图 / 原型并对 `08 §7.9` 四条拍板
  （① D-UI-A vs D-UI-B；② `#sim-run` 是否留全局带；③ 窄窗口 `⋯` vs 折行；④ 刻度 28·24·4·12）。
- **真机 P-UI-02 真 bug 未修**（1440 / 1280 工具带越界）—— 排期在 **U3**：必须先 U2 把工具带
  spec 数据驱动化，才好在真机按优先级整组收进 `⋯`。
- `#95（G1~G8）` 仍为用户明确暂缓（方案保留在 08 §6.2，勿删）；`#88~#92` 五项已于第九轮 ✅，本轮实测无回归。

### 交付提醒
- 给用户看：`node tools/dev-server.mjs 8951` → 浏览器开 **`http://127.0.0.1:8951/prototype/ui-mockup.html`**
  （**必须 http**，`file://` 下 `../img/*.svg` 受限）。
- 或直接看截图：`.e2e-tmp/ctl-1680-toolbar.png`、`.e2e-tmp/ctl-1280-ovf-open.png`、`.e2e-tmp/ctl-full-1680.png`。
- 强调：**纯 mock、假数据、零后端、未动 `index.html`/`js/`/`css/`、未重建 exe**。
---

## 第二十五轮（2026-09-13）：U0-R 顶栏统一 + 真机 splitter 模型重写 + 三条真 bug + 混淆残留清零

### 本轮目标（用户四条原话对账）
1. 「**波形的时间标 1234 等错误地放在了源码区**」→ 查真根因并修。
2. 「**编辑栏先不做修改**，原来的 UI 界面中的文件编辑的下一栏，也就是保存图标、撤回图标、放大缩小图标等**还放到此位置**，而不是放在波形区的子栏；**波形区的子波形区现在不添加功能栏**，功能栏仍旧放在统一的顶栏那里」→ 撤销第二十四轮 D-UI-A。
3. 「现在各个窗口的**大小拖拽有 bug，似乎和鼠标坐标对不上**，而且检查显示等其他 bug」→ 查根因（真机 `js/sim/panel-layout.js`）。
4. 「**已有绘图 UI 尽量不做改动**，现在的关于 UI 的代码应该不是混淆过的，直接照搬即可。如果是很混淆过的，请查明问题之前做过的所有代码的解混淆和剔除工作」→ 混淆复查 + 残留清零。

### ① 原型 U0-R：撤销 D-UI-A，顶栏统一
- `prototype/ui-mockup.{html,css,js}`：把第二十四轮**下沉到波形面板带**的 14 个绘图/编辑控件**全部收回统一顶栏** `#toolbar`
  → 5 组 18 个控件（带 `data-ovp` 溢出优先级）。
- **`panelWave()` 整条 `.mk-toolrow` 删除** → **波形区零子功能栏**（用户明令）。
- 其余面板的 `.mk-toolrow` 保留（面板专属动作，不属绘图类）。
- 帮助浮层 §5 + `mk-version` 同步 → `mock-3（顶栏统一 U0-R）`。
- 决策落档：**07 D24**（U0-R 回退决策 + 5 条用户裁决表 + splitter 基线模型技术口径 + e2e 断言口径 + 混淆复查结论）；
  `08 §7.4` 标题改「⚠ D-UI-A 作废 → U0-R」；`08 §7.9-①` 关闭。

### ② 真机 splitter 高度模型根本重写（用户「拖拽和鼠标坐标对不上」的真根因）
`js/sim/panel-layout.js` 旧实现三处叠加缺陷：
1. `onMove` 把**累计位移当增量反复叠加** → 越拖越飞；
2. `weights` 被当**像素**用，但 `applyWeights()` 把它归一化成**占比**；
3. `flex-basis:0` + 占比模型一旦触到 `min-height`，px↔权重**无恒定系数** → 拖拽量与视觉位移脱钩。

新模型：新增 `cardBaseline` / `pairSlackPx` / `resizePairTo` / `resizePair`；
`applyWeights` = `flexBasis: <卡片基线 px>` + `flexGrow: <占比>*100` + `flexShrink: 0`；
**`applyMinHeights` 同源同时写 `minHeight` 与 `flexBasis`**（此前窗口 resize 只重算 min-height → flexBasis 留在旧值，是错位根因之一）；
`onPointerDown` 记录 `startHeightA`，`onMove` 走 `resizePairTo(pair, startHeightA + delta)`。

### ③ 三条真 bug（全部实测定位、修复、回归）
| # | 现象 | 真根因 | 修法 | 证据 |
|---|------|--------|------|------|
| a | **时间标 1234… 跑进源码区** | 原型 `.mk-gutter` 缺 `white-space:pre` → 行号换行吃掉宽度 | 加 `white-space:pre` | `gutW 369→32`、`bodyW 59→396`，修复后 `ticksInSource === 0` |
| b | 波形刻度与菜单勾选样式串味 | 原型类名冲突 `.mk-tick` | 波形刻度改 **`.mk-wave-tick`** | mock-probe 过 |
| c | 原型信号名列宽拖拽完全无效 + 时间轴/波形轨横滚不同步 | `closest` 命中错元素 | `closest` → `querySelector`；`tracks.scrollLeft` → `axisScroll.scrollLeft` | mock-probe 过 |

### ④ 混淆复查 + 残留清零（触发 C1 → exe 已重建）
- 复查 `js/wavepaint.clean.js`：**无混淆残留** —— `_0x55bf` / `\xNN` 等要么在注释、要么是 ✕ 字符/产品功能；`lib/*.min.js` 是第三方正常压缩（属正常资产，非混淆代码）。
- **另清除 9 个残留 `_0x_` 前缀局部变量**（`_0x_wpfQuickCleanup` / `_0x_wpfSuppressBlur` / `_0x_wpfMarkSuppress` / `_0x_wpfClearError` / `_0x_wpfOnBlur` / `_0x_wpfText` / `_0x_wpfOnInput` / `_0x_wpfOpts`，共 32 处）→ 改名 `wpf*`；**正文 `_0x` 归零**（仅剩文件头注释 3 处历史说明）。
- 此前第七/第八轮的解混淆与死代码剔除成果**仍然有效**，本轮为收尾清扫。

### 实测结果（全部实跑，均为本轮最终态）
| 套件 | 命令 | 结果 |
|------|------|------|
| UI 回归 | `node tools/e2e-ui.mjs` | **88/88**（I1 改口径 + 新增 **I4c 指针拖拽**） |
| 纯函数回归 | `node tools/regression.mjs` | **79/79** |
| RTL 交互 | `node tools/e2e-rtl.mjs` | **61/61** |
| 仿真端到端 | `node tools/e2e-sim.mjs` | 失败 0 / 已知缺陷 0 |
| 原型探针 | `node tools/mock-probe.mjs` | **34/34**（连跑 3 次稳定） |
| 原型控件审计 | `node tools/ui-audit.mjs --mock` | 1680 / 1280 两档违规 **0** |
| 真机控件审计（未修） | `node tools/ui-audit.mjs --real` | 1920 违规 1 / 1440 违规 3（溢 32px）/ 1280 违规 3（溢 192px）= **U3 范围** |

**e2e-ui 本轮三条断言改动**：I1 改口径（基线模型 = `flex-basis` 卡片基线 px + `flexGrow` 占比 + `flexShrink 0`）；新增 I4c（指针拖拽：拖 DY → 上下卡各 ±DY 且总高守恒，同手势拖回高度可逆）。

### C1 / exe
- **C1 触发**（改了 `js/`）→ **exe 已重建**：
  `WavePaintClean.exe` = **22,015,488 B / 2026-09-13 16:52:06**；
  `version.txt` = `v0.4.0 build 2026-09-13 16:52:06 83fab85`（`83fab85` = **构建时 HEAD**，承载本轮代码的 commit 是它的**下一个**，勿误判落后）；`resources.txt` = 52 项。
- **C8 特征串核验通过**：`cardBaseline`=7、`pairSlackPx`=3、`resizePairTo`=3、`applyMinHeights`=7、`sim-resize-x`=12、`wpfQuickCleanup`=5、**`_0x_wpf`=0** ✅。

### 红线守住
- **未改 `js/wavepaint.clean.js` 绘制层与 `js/sim/engine.js` 一行**（C9）；弹窗仍走 `__core.prompt`（C10）；
- 24 个冻结 id 一个未改名/未删除；`#sim-status` 常驻可见、`#sim-run` 始终可点；
- **波形区不加子功能栏**（U0-R 裁决，已写入 08 §7.10 红线）。

### 遗留 / 下一步
- **等用户 review 第二十五轮原型**（`node tools/dev-server.mjs 8951` → `http://127.0.0.1:8951/prototype/ui-mockup.html`，必须 http）。
- 未提异议即按 `08 §7.8` 总排期推进：**U2（工具带 spec 数据驱动化）+ D0（面板注册表，零视觉变化）合并同批** → **U3 真机落地 + P-UI-02 修复** → D1 → D2 → D3 → D4+U4。
- **D-UI-A 已作废**：真机 21 个图标按钮**不下沉**，全部留在 `#toolbar`；U3 只做分组 / 刻度 / 尺寸 / `⋯` 溢出。

---

## 第二十六轮（2026-09-13）：原型打包为可双击运行的 exe —— 交付方式从「网页链接」改为「零依赖 exe」

### 本轮目标
- 用户原话：「**提供的网页链接无法打开**，请按照**原本之前的方式**一样打包成一个**可直接运行的点 exe** 供我 review」。
- 判定 = **交付方式问题，不是原型本身问题**：第二十三~二十五轮的 review 入口是「起 `node tools/dev-server.mjs 8951` + 手输 `http://127.0.0.1:8951/prototype/ui-mockup.html`」→ 用户打不开。
- 目标：双击 exe → 自动起本地静态服务 → 自动打开 Edge **无地址栏**窗口显示 U0-R 原型；关窗即退；**零依赖、零命令行、不引入新工具**。

### 做了什么
1. **新增 `MockupLauncher.cs`（528 行）** —— 原型专用启动器，**独立于真机 `WavePaintLauncher.cs`（后者一行未改）**：
   - 自包含静态资源 HTTP 服务，端口优先 **17820**（真机 17817），被占则 `FreePort()` 换随机端口；绑定 `http://127.0.0.1:<port>/`。
   - `/api/ping` → **`WAVEPAINT-MOCKUP <构建戳>`**（真机是 `WAVEPAINT-SERVICE`，身份不混）；`/mockup-version.txt` 返回构建戳。
   - `MapPathToResource`：`/` → `prototype/ui-mockup.html`；`ui-mockup.*` → `prototype/`；其余按内嵌资源名；**含 `..` 一律 404**。
   - 单实例发现 `%TEMP%\WavePaintMockup_service.txt`（`tag=` / `port=`）+ `/api/ping` 判活（第二实例自动交接退出）。
   - 开窗 = Edge `--app="http://127.0.0.1:<port>/prototype/ui-mockup.html"`；无 Edge 退默认浏览器。
   - 窗口判定 = `EnumWindows` + `GetWindowText`，标题含「**原型**」（真机标题含 WavePaint 但不含「原型」→ 互不误伤）；无窗口连续 **12s** → 退出；最长存活 **12h**。
   - 参数 `/nolaunch`（只起服务不开窗）、`/port=N`。
2. **新增 `build-prototype.ps1`（64 行）** —— 独立构建脚本：**不写 `resources.txt`、不动 `version.txt`、不碰 `WavePaintClean.exe`**；内嵌 `prototype/**` + `img/**` + `mockup-version.txt`；`csc /target:winexe /win32icon:img\app.ico /r:System.Windows.Forms.dll`；产物 `WavePaintMockup.exe` + `mockup-version.txt`。
3. **新增 `tools/mockup-exe-smoke.mjs`（143 行）** —— 无浏览器自检：`/nolaunch` 起服务后核验 ping 身份 / 首页与静态资源 状态码·Content-Type·**字节数与磁盘源码逐一相等** / 构建戳 / 404 分支 / HEAD / 4 条原始 socket 路径穿越非 200。
4. **`tools/mock-probe.mjs`（改）** —— 新增 `MOCK_BASE`（`argv[2]` / `process.env.MOCK_BASE`）：同一套 34 项探针既可打 dev-server（默认 8951），也可打 exe 服务。
5. **`prototype/ui-mockup.html`（改，+12 行）** —— 末尾 `<script>` 抓 `/mockup-version.txt`，成功则把构建戳写进顶部绿条 `#mock-version`（dev-server 下 404 静默跳过）。
6. **`.gitignore`（改，+2 行）** —— 忽略 `WavePaintMockup.exe` / `mockup-version.txt`。

### 关键结论
- **四重身份隔离**（原型 vs 真机）：端口 17820/17817、ping 串 `WAVEPAINT-MOCKUP`/`WAVEPAINT-SERVICE`、单实例文件 `WavePaintMockup_service.txt`/`WavePaint_service.txt`、窗口判定「原型」/`WavePaint`。**两者可同时运行、互不影响。**
- **exe 内是构建时快照**：改了 `prototype/**` 必须重跑 `.\build-prototype.ps1`，否则 review 到的还是旧界面。
- **`build-prototype.ps1` 必须带 UTF-8 BOM**（PS5.1 读无 BOM 的 `.ps1` 会把中文源码读成乱码）；⚠ 变量名不能叫 `$home`（PowerShell 只读变量）→ 用 `$homePage`。（已写入 06 P38）
- **Edge `--app` 窗口不能靠 `Win32_Process.CommandLine` 判定**（只有根进程带 `--app`，子进程不带）→ 必须用窗口标题 `EnumWindows` 判定。（已写入 06 P38）
- **C1 未触发**：只碰 `prototype/` + `tools/` + 新增根目录两个非产品文件；真机 `js/`/`index.html`/`css/`/`img/`/`lib/`/`WavePaintLauncher.cs`/`build.ps1` 一行未改 → **真机 exe 未重建**。

### 验证结果（全部实跑）
| 项 | 结果 |
|---|---|
| `build-prototype.ps1` | `csc exit: 0` / `res count: 27` / `WavePaintMockup.exe` = **186,368 B**（2026-09-13 17:08:12） ✅ |
| `node tools/mockup-exe-smoke.mjs` | **41/41 ✅** |
| `node tools/mock-probe.mjs`（源码 dev-server） | **34/34 ✅**（默认行为不变） |
| `node tools/mock-probe.mjs http://127.0.0.1:17820`（**对 exe 服务**） | **34/34 ✅** |
| `node tools/ui-audit.mjs --mock` | 违规 **0** ✅ |
| 真实双击路径 | `%TEMP%\WavePaintMockup_service.txt` = `tag=WAVEPAINT-MOCKUP` / `port=17820`；ping 通；可见窗口标题「WavePaint UI 原型（假界面 · 未连接后端）」；截图 `.e2e-tmp/mockup-exe-window.png` 1050×999 / 207 色（非白屏）；WM_CLOSE 关窗 → **第 12 秒自动退出**；第二实例自动交接、第一实例仍在 17820 ✅ |

### 遗留问题
- **等用户 review `WavePaintMockup.exe`**（本轮唯一交付物）。
- 真机 **P-UI-02**（1440 `#toolbar` 溢 32px / 1280 溢 192px）仍未修，排期 **U3**。
- 用户通过后按 `08 §7.8` 总排期：**U2（工具带 spec 数据驱动化）+ D0（面板注册表，零视觉变化）合并同批** → **U3 真机工具带落地 + P-UI-02** → D1 停靠引擎 → D2 页内浮动 → D3 持久化 → D4+U4。

### 交付提醒
- **原型**：双击 `D:\Files\Code\波形\WavePaintMockup.exe` —— **纯 mock / 零后端 / 假数据**；端口 17820；窗口顶部绿条显示 `v0.4.0-mock build 2026-09-13 17:08:12 2d1636a`；关窗约 12 秒后进程自动退出；**不需要 Node、不需要命令**。
- **真机**：仍是 `D:\Files\Code\波形\WavePaintClean.exe`（`v0.4.0 build 2026-09-13 16:52:06 83fab85`，**本轮未重建**）；与原型可同时运行、互不影响。

---

## 第二十七轮（2026-09-13）：拖拽两条真 bug 修复 + 原型「1:1 完全照搬真机」重构 + 审计口径校准 + exe 重打包

### 本轮目标（用户原话对账）
- 用户原话：「目前各个面板的**拖动仍然有问题，拖动的预览和最后实际的效果不一致**，且**拖动按钮在拖动时的显示也有 bug**。此外，代码显示区和菜单栏的显示等**并没有照搬原本的源码**，我希望**完全照搬它的源码，做出 1:1 的效果**，只是原本是全屏的，现在是面板而已，而不是按照预测的显示效果等进行复刻，我要 **1:1 完全一样的效果**，完全一致的效果**起码波形显示区是这样的**。」
- 判定三件事：① 两条拖拽 bug = **同根因的真 bug**（不是「手感问题」），必须修根因 + 加回归；② 「1:1」= **口径裁决**（07 D26）—— 原型不再是「按预期效果另画一版」，而是**逐字照搬真机源码 / 样式 / 脚本**；③ 该口径顺带推翻旧审计规则对原型的适用性 → 校准 `tools/ui-audit.mjs`。

### ① 拖拽两条真 bug（同一根因；06 P39 / 07 D27）

**根因（一句话）**：拖拽闭包**缓存了 DOM 引用**，而拖拽中途 `afterLayout()` 的 `setTimeout(60ms) → window.dispatchEvent('resize')` 会让原型 `render()` / `renderFloats()` **重建整棵 DOM** → 闭包里的 `el` / `handle` / `slots` 变成**游离节点**，样式与类名写进空气。

| 症状 | 机制 | 实证 |
|---|---|---|
| 拖动预览 ≠ 松手后落位 | 往游离 `el` 写 `style.transform` / `dataset` 不生效 | 探针日志 `[fdrag.up] elConnected=false sameEl=false sameF=true`（f.x/f.y 更新了但写在未连接节点上） |
| 拖动中 `⧉ ▣ ✕` / 页签 `✕` 乱闪 | 用游离 `el.classList` 判拖动态 → CSS `body.mk-dragging … opacity:0 !important` 失效 | H4/H5 加断言后可见 |

**修法（`prototype/ui-mockup.js`，三条铁律 → 07 D27）**：
1. **稳定 id**：`split()` / `cloneTree()` 给 split 节点补 `id = 's-' + (++seq)`（原来只有 tabs 有 id），`buildNode()` 落 `el.dataset.splitId`；浮窗本就有 `data-float`。
2. **每次查活节点**：`startSplitDrag` / `startFloatDrag` / `startFloatResize` 的 `move` 一律 `liveSplit()` / `floatsLayer.querySelector('.mk-float[data-float="id"]') || el` **现查**，绝不用缓存引用。
3. **监听挂 `window`**：`pointermove` / `pointerup` / `pointercancel` 从 handle 改挂 `window`（手柄被换掉也不中断），补 `pointercancel` 取消分支；跨重建的拖动态用**模块级变量** `draggingFloatId`（`renderFloats()` 按它拼 `.dragging` 类）；松手统一 `renderFloats()` + `persist()` 收尾。

**新增样式（`prototype/ui-mockup.css`）**：`.mk-float.dragging{ z-index:500; border-color:var(--accent); box-shadow:… }`、`.mk-float.dragging .mk-float-head{ background:var(--active-bg) }`、拖动中 `cursor:grabbing !important` + `.mk-float-body{ pointer-events:none }`。

**新增回归（`tools/mock-probe.mjs` I 段 5 例，专打「拖拽中途被 render 重建」）**：

| 例 | 断言 | 结果 |
|---|---|---|
| I1 | 重建后仍在拖动状态（`body.mk-dragging` + `.mk-float.dragging`） | `{"swapped":true,"oldConnected":false,"dragging":true,"bodyDrag":true}` ✅ |
| I2 | 重建后松手：落位 == 拖动中最后位置 | 拖动中 `{x:390,y:944}` == 松手后 `{x:390,y:944}` ✅ |
| I3 | 松手后清理拖动态 | ✅ |
| I4 | 分隔条拖动中 `render()` 重建后仍跟随指针 | `314 → 494`（期望 +180）✅ |
| I5 | 分隔条重建后「拖动中 == 松手后」逐像素 | `[494,730,430]` == `[494,730,430]` ✅ |

（另有既有 **H1~H5**：H1 分隔条「拖动中==松手后」逐像素 `[404,820,430]`、H2 只改相邻两格、H3 +90px、H4/H5 拖拽中 `⧉▣✕` 与页签 `✕` `opacity=0`；**G 段 7 例**：预览矩形 == 落位矩形逐像素相等（波形组左/右/上/下/中心、工作区左外环、底外环）。）

### ② 原型「1:1 完全照搬真机」重构

**新增 `tools/gen-mock-page.mjs`（原型页生成器，唯一正确来源 = 真机 `index.html`）**：
1. 读真机 `index.html` **全文**（只读，绝不写回）；
2. **机械替换**资源路径：`css/ js/ lib/ img/` → `../css/ ../js/ ../lib/ ../img/`（原型页在 `prototype/` 子目录，URL 层级必须与真机一致，否则 `js/sim` 的 ESM 相对 `import` 会 404）；
3. 内联 module 说明符 `'./js/sim/project-model.js'` → `'../js/sim/project-model.js'`（module 的 import 按**文档 URL** 解析）；
4. 真机 head 内联 `<style>` 之后插一行 `ui-mockup.css`（原型外壳样式**只在最后覆盖**，不改真机任何元素的原生规则）；
5. `</body>` 前追加 **`prototype/mock-tail.html`**（工作区宿主 / 状态栏 / 帮助浮层 / `ui-mockup.js`）。

**新增 `prototype/mock-tail.html`**：原型独有的「外壳」（工作区宿主 `#mk-park`、状态栏、帮助浮层、拖拽引擎脚本引用）—— 与真机 chrome 彻底分离，避免混进真机源码。

**结果**：`prototype/ui-mockup.html` = 64,658 B / 1,133 行（真机骨架逐字 + 原型外壳），**菜单栏 / 工具带 / 代码区（CodeMirror 宿主）/ 波形显示区 = 真机原样 DOM + 真机原样 CSS + 真机原样 JS 资源**。

**真机 vs 原型 1:1 比对（`node .e2e-tmp/r37e.mjs`）= 13 same / 1 diff**：

| 维度 | 结论 |
|---|---|
| `#menu-bar` / `#toolbar` outerHTML（归一化 `../img/` 后） | **SAME** |
| `#menu-bar` / `#toolbar` 矩形、菜单项(4) 矩形、工具条子元素(**35**) 矩形 | **SAME** |
| CodeMirror 源码文本 / 行数 / 字体·颜色·背景 | **SAME** |
| `verilog-source` 兜底值 / `rtl-tree` 前缀 / `sim-status` 文本 / 主题 CSS 变量 | **SAME** |
| sim 卡片 id 集合 | **DIFF**（仅 DOM 顺序：真机 `[source,rtl,vcd,tb]` vs 原型 `[rtl,source,vcd,tb]`，**无害**） |
| 画布尺寸 | 真机 `1540×922` vs 原型 `1540×609`（高度差 = 面板高度，**宽度相同**） |

**像素 diff（`node .e2e-tmp/r37f.mjs`，同一页面内 canvas 比对）**：

| 区域 | 结果 |
|---|---|
| wave canvas `908×609` | `diffPx=78/552972 = **0.0141%**`，`maxDelta=31`（只差网格虚线抗锯齿亚像素 `(240,240,240)` vs `(242,242,242)`） |
| chrome band `1680×86` | `diffPx=681/144480 = **0.4713%**`，`maxDelta=11` |

→ **波形显示区 1:1 达成**。

**真机黄金基线（1680×1000 headless，供日后回归）**：`#menu-bar` l=0 t=0 w=1680 **h=40**；`#toolbar` l=0 **t=40** w=1680 **h=46**；`#main-area` t=86；原型 `#workbench` t=86 h=888、`#status-bar` t=974 h=26；波形面板内 `#wave-view` 908×609、`#wave-canvas` 1540×609。原型布局树：`col[0.74,0.26]` → `row[0.19,0.55,0.26]`（z / rtl+vcd ｜ wave ｜ source+tb）+ z-4 console。

**关键约束（必须遵守）**：
- **绝不能改 `css/` / `js/` / `lib/` 下任何文件**（真机 ESM 相对导入，URL 路径必须与真机一致）；**`js/sim/panel-layout.js:72 if (!cards.length) return null;`** → 4 张卡片被原型搬走后 `panelLayout` 为 null，**无需改任何 js**。
- **U0-R 仍在生效**：波形区**不得加子功能栏**；绘图/编辑控件全留统一顶栏 `#toolbar`；**已有绘图 UI 照搬不改**。

### ③ 审计口径校准（07 D26）

`tools/ui-audit.mjs` 的 **R5 间距刻度 / R6 禁隐式溢出 / R8 不被裁切** 三条改为**只在 `target==='real'` 判定**；mock 豁免（原型继承真机原生间距 `{4,6,8,10,12,18}`、真机 1280 `#toolbar` 溢 192px、步数/子步数越界 —— 这些是真机待优化项 **P-UI-02**，属 U3，**不是原型缺陷**）。mock 仍保留 **R3 主按钮唯一 / R6 控制带禁换行 / R9 提示不进工具带**。

**理由（用户口径）**：「1:1 完全一致」优先于旧审计口径；**绝不为迁就旧审计去改真机 DOM / 间距 / toolbar 行为**。调用处由 `violationsOf(a)` 改为 `violationsOf(a, name)` 并加一行提示文案。

### ④ exe 重新打包

- `build-prototype.ps1` 内嵌目录由仅 `prototype/` 扩为 **`prototype/ img/ css/ js/ lib/`（res count 27 → 54）** → `WavePaintMockup.exe` **186,368 B → 1,709,056 B**（`mockup-version.txt` = `v0.4.0-mock build 2026-09-13 18:55:12 b93ad07`）；`MapPathToResource` 按逻辑名命中，**C# 一行未改**。
- ⚠ **打包前必须关掉正在运行的旧 exe**：本轮首次 `csc` 失败 `CS0016 未能写入输出文件…另一个程序正在使用此文件`（用户上轮 review 留下的进程 PID 47024/70792）→ `taskkill /F /IM WavePaintMockup.exe` 后 `csc exit: 0`。
- `tools/mockup-exe-smoke.mjs` 同步：资源清单加 `css/wavepaint.e7b903ef.css`、`js/wavepaint.clean.js`、`js/sim/ui-bridge.js`、`js/sim/engine.js`、`lib/codemirror.bundle.js`（**与磁盘逐字节比长度**）；首页断言由 `mock-banner` 改为 **`#workbench` + `#mk-park` + `#menu-bar` + `#toolbar`**；404 分支改 `/js/sim/engine.js.bak`（内嵌 `js/` 后 `/js/core/__core.js` 已变 200）→ **56/56 ✅**。

### 验证结果（全部实跑，均为本轮最终态）

| 项 | 命令 | 结果 |
|---|---|---|
| 原型探针（源码 dev-server） | `MOCK_BASE=http://127.0.0.1:8961 node tools/mock-probe.mjs` | **55/55**，失败 0 ✅ |
| 原型探针（**exe 服务**） | `node tools/mock-probe.mjs http://127.0.0.1:17899` | **55/55** ✅ |
| 控件审计（原型） | `node tools/ui-audit.mjs --mock` | 1680 / 1280 **两档违规 0** ✅（D26 新口径） |
| exe 无浏览器自检 | `node tools/mockup-exe-smoke.mjs` | **56/56** ✅ |
| 真机 vs 原型 1:1 | `node .e2e-tmp/r37e.mjs` | **13 same / 1 diff**（diff = 卡片 DOM 顺序，无害） ✅ |
| 像素 diff | `node .e2e-tmp/r37f.mjs` | 波形画布 **0.0141%** / chrome band **0.4713%** ✅ |
| 落点预览矩阵 | `node .e2e-tmp/r37c.mjs` | 10 例全过 ✅ |
| 分隔条 + 拖拽按钮 | `node .e2e-tmp/r37d.mjs` | 8 例全过 ✅ |

关键 PASS 行：
```
PASS H1 分隔条拖动中 == 松手后（逐像素）  → 拖动中=[404,820,430] 松手后=[404,820,430]
PASS H2 分隔条只改相邻两格（第三格不变）  → 前=[314,910,430] 后=[404,820,430]
PASS I1 {swapped:true,oldConnected:false,dragging:true,bodyDrag:true}
PASS I2 拖动中={x:390,y:944} 松手后={x:390,y:944,...,dragging:false,bodyDrag:false}
PASS I4 分隔条拖动中 render() 重建后仍跟随指针  → 314 → 494（期望 +180）
PASS I5 拖动中=[494,730,430] 松手后=[494,730,430]
```

### 改动文件

| 文件 | 改动 |
|---|---|
| `prototype/ui-mockup.js` | **核心修复**（稳定 id + 现查活节点 + 监听挂 window + `draggingFloatId`） |
| `prototype/ui-mockup.css` | 新增 `.mk-float.dragging` 系列样式 |
| `prototype/ui-mockup.html` | **由 `tools/gen-mock-page.mjs` 重新生成**（真机 1:1 骨架，64,658 B / 1,133 行） |
| `prototype/mock-tail.html` | **新增**（原型外壳：工作区/状态栏/帮助浮层/拖拽引擎） |
| `tools/gen-mock-page.mjs` | **新增**（真机 `index.html` → 1:1 原型页生成器） |
| `tools/mock-probe.mjs` | 新增 **I1~I5** 五条拖拽重建回归；`E2` 过滤 `net::ERR_ABORTED`（导航取消 ≠ 加载失败） |
| `tools/mockup-exe-smoke.mjs` | 资源清单加 css/js/lib + 首页断言改真机骨架四件套 + 404 分支调整 → 56/56 |
| `tools/ui-audit.mjs` | **D26 口径**：R5/R6 隐式溢出/R8 只在 `real` 判定 |
| `build-prototype.ps1` | 内嵌目录扩为 `prototype/ img/ css/ js/ lib/` 五目录 |

### 红线守住

- **真机一行未改**：`js/`、`index.html`、`css/`、`img/`、`lib/`、`WavePaintLauncher.cs`、`build.ps1` 全部未动 → **C1 未触发**，真机 `WavePaintClean.exe` 仍 22,015,488 B / 16:52:06、`version.txt` 仍 `v0.4.0 build 2026-09-13 16:52:06 83fab85`。
- 24 个冻结 id 全在（`sim-panel`/`verilog-source`/`verilog-cm-host`/`rtl-tree`/`vcd-tree`/`tb-source`/`sim-status`/`sim-recover`/`app-version` …）。
- 不改绘图 UI；不动 `js/sim/engine.js`（C9）；不新建弹窗入口（C10）。
- `prototype/`、`tools/`、`memory/` 不进 `resources.txt`。

### 遗留问题

- **等用户 review `WavePaintMockup.exe`（第二十七轮版）** —— 重点验：① 面板拖动「预览 == 落位」；② 拖动中按钮/页签无乱闪；③ 菜单栏 / 代码区 / 波形显示区与真机 1:1。
- **生成器与打包的联动顺序**：改真机 `index.html` 骨架后，**必须先跑 `node tools/gen-mock-page.mjs` 再跑 `.\build-prototype.ps1`**，否则 exe 里仍是旧原型页。
- 真机 **P-UI-02**（1440 `#toolbar` 溢 32px / 1280 溢 192px）仍未修，排期 **U3**。
- 用户通过后按 `08 §7.8` 总排期：**U2 + D0 合并同批** → U3（含 P-UI-02）→ D1 停靠引擎 → D2 页内浮动 → D3 持久化 → D4+U4。

### 交付提醒

- **原型**：双击 `D:\Files\Code\波形\WavePaintMockup.exe`（**1,709,056 B / 2026-09-13 18:55:12**）—— 纯 mock / 零后端 / 假数据；端口 17820；窗口顶部绿条显示 `v0.4.0-mock build 2026-09-13 18:55:12 b93ad07`；关窗约 12 秒后进程自动退出；**不需要 Node、不需要命令**。
- **重新打包前先 `taskkill /F /IM WavePaintMockup.exe`**（否则 `csc` 报 `CS0016`）。
- **真机**：仍是 `D:\Files\Code\波形\WavePaintClean.exe`（`v0.4.0 build 2026-09-13 16:52:06 83fab85`，**本轮未重建**）；与原型可同时运行、互不影响。
