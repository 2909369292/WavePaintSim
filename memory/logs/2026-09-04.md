# 2026-09-04 WavePaint 记忆与项目整理

## 本轮目标（用户指令）
1. 软件里不再单独强调"解引用/clean"——只当普通软件；解引用只记在日志/记忆。
2. 整理项目文件结构（不改代码、不改量名/文件名）。
3. 把重要记忆单独列制成一个文件（跨 AI 可读取）。
4. 整理需求列表（完成情况+实现思路），作为可同步更新的记忆。
5. 以上记忆文档支持跨 AI 新对话直接调用。

## 已做

### 去"解引用/clean"强调（任务 #54）
- 运行 UI（index.html `<title>`=「WavePaint 波形编辑」）本就无强调；launcher 的
  "WavePaintClean" 仅是 exe/临时文件/消息框内部名（用户要求不改文件名/变量名，保留）。
- **README.md 重写**：去掉标题「WavePaintClean — 混淆核心解混淆 + 重构」与「## 解混淆」整段，
  改为普通软件 README（是什么/功能/目录结构/构建/使用），仅在「维护背景」一句 factual 提及
  核心由混淆代码还原、细节见记忆。docs/ 内的解引用叙事属开发记忆，保留。

### 整理项目文件结构（任务 #55）
- 根目录零散构建日志（`build.log` / `build_9月2日*` / `build_phase4_*`）归入新建 `logs/`。
- README「目录结构」更新为合并后实际结构（仓库根即项目本体，无子项目目录；去掉 `../WavePaintSim` 等失效引用）。
- 说明：`index.obf.html`（旧版对照页，引用原始混淆核心）作为历史遗留保留；`WavePaintSim` 空残留目录
  仍被本会话句柄 + safe-delete 锁目录失败删不掉，内容已入隔离区，无害。

### 重要记忆单列成文件（任务 #56）
- **重写 `.workbuddy/memory/MEMORY.md`** 为跨 AI 记忆总纲：
  - 〇 项目定位（普通软件；核心由混淆还原属内部事实，仅记记忆）；
  - 一 **用户硬性特殊需求（铁律）**：改任意被嵌入 exe 的代码 → 必须重建 exe 并 commit+push main；
    构建前直接 `taskkill /F /IM WavePaintClean.exe`（无需确认）；交付后提醒用户从唯一路径重启；
    用户报 bug 本地全绿先查旧副本；
  - 二~十 沿用并修正旧坑（build BOM / 编译器失败码 / exe 内嵌 / 仿真禁区 / 数据模型 / 验证门槛 /
    主题 / 进制标签弹窗 / 磁盘验证 / 交付流程 / 用户约定），修正失效的"WavePaintSim 子目录"引用；
  - 十一 文件结构与模块职责（整理后）；十二 跨 AI 使用说明。

### 需求列表（任务 #57）
- **新建 `.workbuddy/memory/REQUIREMENTS.md`**：汇总 #1~#58 全部需求，按
  A 功能 / B Bug / C 仿真 / D 解混淆重构 / E 项目治理 五类，列「需求/状态/实现思路」，
  标注 #54~#58 为本轮完成。末尾列待办方向。要求以后变更需求同步更新本文件。

### 跨 AI 落地（任务 #58）
- MEMORY.md + REQUIREMENTS.md 自包含、位于 `.workbuddy/memory/`，任何新对话/AI 直接读取即可承接。
- 本轮改动（README 重写、记忆整理、结构梳理）提交 git 并 push main。

## 提交
- 待提交：README.md（去强调）、.workbuddy/memory/MEMORY.md、REQUIREMENTS.md、2026-09-04.md、
  logs/（gitignored，不进库）。随后 `git push` 到 origin main。
- 重建 exe：本轮未改任何会被嵌入 exe 的代码（js/index.html/css 等未动），**无需重建 exe**。

## ⚠ 给下一位 AI 的铁律（复述）
改 `js/ index.html css img lib WavePaintLauncher.cs` 中任一 → 必 `.\build.ps1` 重建 exe → 必 commit+push main。
构建前 `taskkill /F /IM WavePaintClean.exe`。详见 MEMORY.md 第一节。

---

# 2026-09-04 第二轮：正确性收口 + 体验补强 + 永不掉线（批次1~10，需求 #59~#72）

用户指令：一次对话内完成阶段1（修 bug+收口）、阶段2（体验补强，仅数字波形）、
仿真服务永不掉线、混乱代码重构、阶段3远期规划；每步验证+git 记录。全部完成。

## 批次提交记录（每批独立 commit，全量验证后提交）
1. fc0b61b 私有 divisor 全链路收口：resize 每信号 divisor 重建+锚点重映射；
   generator/templates 收敛 wpf.stride()（修 (x||1)+1 复活 bug）；sim 按
   signalDivisor/cellStride 采样；engine NaN 防御；两处自建 modal 收编
   wpf.openFormDialog（Enter/Esc/遮罩/红框）；删 isAlternatingCells
2. 550c6fb 值解析统一+交互稳健性：parseValue 按 radix（dec 的 10 不再被位串遮蔽、
   hex 数字串按 16 进制、裸 hex 全串校验）；value-input 文档代际守卫+会话结束
   切回画笔；selection 合成 Esc 走核心状态机+setPointerCapture；shortcuts
   Escape 清光标+快照 800ms 合并+undo 走 API；measure 光标存 mainStep（二分反推 x）；
   __core.ready 兜底轮询+state() 全守卫；heartbeat 前置（此批内含于批次2+3）
3. bc70740 仿真解析健壮性：normalizeSignalName 长前缀在前；ANSI 多关键字循环剥离；
   参数化位宽代入求值（求不出回退标量，绝不进 TB）；位宽兼容守卫
4. 293b817 launcher 永不掉线：AcceptLoop 自愈（异常→同端口重建循环）；simActive
   Interlocked 计数；no-cache；快照上限；QuoteArg 反斜杠规则；退出语义收口
5. 6641d18 死代码清理（engine 4 函数）+ docs/attic 归档 10 份旧文档
6. 4511430 文件菜单复活（.wp 工程存取）+ R6 实时预览 + 任意工具 Ctrl+C/V
7. 01ee9b2 DUT 内部信号回显 + 顶层模块选择器 + runSimulation 防御
8. ac42ef0 版本显示（version.txt 内嵌+面板展示）

## 重大发现
- **文件菜单全死**：核心 initMenuHandlers 按英文文本（'New'/'Open'/'Save As'）绑定，
  index.html 汉化后 5 项全部无响应 → 核心自带的 .wp 工程保存/加载/分享能力不可达。
  file-menu.js 按中文重绑修复。「新建」必须就地重置（核心只赋内部词法变量，
  换 window 对象会身份撕裂 —— 因菜单死而从未暴露的潜伏 bug）。
- **快捷键 Ctrl+C/V 核心本就有**（select 工具 + rangeSelActive 门控），shortcuts.js
  补任意工具路径（select 下放行防双写；pasteClipboard 有空剪贴板守卫）。

## 验证门槛执行记录
- 每批：node --check 全过 + regression（37→46 项，新增 cellStride/NaN 防御/
  parseValue 语义/多关键字/参数化位宽/前缀顺序/位宽兼容/VCD 回显 8 组）+ e2e-sim 0 失败
- e2e-ui（真实 Edge）：35/35（含 E 组框选回归，全部私有 divisor 用例）
- exe 重建两次（批次5、收尾）；version.txt 无 BOM；exe 冒烟通过
  （全局齐备/汉化弹窗正常/无异常）；/version.txt 端点实测返回正确
- **交付**：WavePaintClean.exe 唯一路径 D:/Files/Code/波形/WavePaintClean.exe
  （v0.4.0 build 2026-09-04 03:31:23 01ee9b2），提醒用户重启并从唯一路径启动

## 环境坑（新记）
- version.txt 用 Set-Content -Encoding UTF8 会带 BOM → 前端多出 \uFEFF；
  改 [IO.File]::WriteAllText + UTF8Encoding($false)，前端再防御 strip。
- 空残留目录 WavePaintSim 本会话仍被 shell cwd 占用删不掉（shell 每次重置回它），
  需用户手动或换会话 rmdir。
