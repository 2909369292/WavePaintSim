# 2026-09-10 WavePaint 工作日志

## 第十三轮（插队专项）：#86 服务在线性根治 —— 本地仿真服务自愈（2026-09-10）

## 本轮目标

1. 根治用户报障：「本地仿真失败，反正请求无响应的问题」「每次点击仿真的时候必须调用仿真
   服务，必须仿真成功」——不是加提示、加重试，而是把「服务不在线」这件事本身消灭掉。
2. 顺手清理上一轮遗留：换版本后首次启动必崩（见下「根因 ④」），这次必须一并修掉。

> 本轮是**服务在线性（availability）专项**，不是新功能；#86 / #76 主线**未开工**。

---

## 一、根因分析（先说结论，四条并存，缺一不可）

页面与本地仿真服务是「同一台机器上的两个进程」：页面由 exe 内嵌 HttpListener 提供，
页面所有 `/api/sim` 都打在 exe 的端口上。历史上「点仿真无响应」有 **四类** 独立死因：

1. **进程死**：exe 退出/被杀 → 端口上没东西监听 → `fetch` 立刻
   `ERR_CONNECTION_REFUSED`。旧实现只弹一句「请重启应用」——用户能做的只有关掉重开。
2. **换端口**：旧版每次启动随机端口 → 已经打开的页面（origin 钉死在旧端口）永远连不上，
   刷新也没用（刷新还是那个死 origin）。
3. **端口被占**：别的程序占了同一端口 → 页面把 `/api/sim` 发给了「别人的」本地服务，
   拿到 404/乱码，报错还很难看。
4. **换版本后首次启动必崩（本轮新发现，真凶之一）**：`WavePaintLauncher.cs` 里 `ivlRoot`
   **只在 cache 命中分支**被赋值；换版本后首次启动走的是 extract 分支 → `Path.Combine(null, …)`
   → `ArgumentNullException` → **服务根本没起来**。日志实证（`%TEMP%\WavePaintClean_sim.log`）：
   - `19:57:24  BOOT resources=extract` → `IVL repair failed: ArgumentNullException path1`
     （旧代码：崩在资源准备，服务未监听任何端口）
   - `20:06:38  BOOT resources=extract`（修复后）→ 直接 `SERVER start pid=11464 port=17817`
     + 首次仿真 `RUN compile primary 0` / `RUN vcd 985` —— **换版本首次启动不再崩**。

> 这条也解释了「我什么都没改，重启一下就仿真不了了」的历史反复：**每一次 exe 重建**
> 都会让下一轮启动走 extract 分支，旧代码就必崩一次。

---

## 二、做了什么

### A. `WavePaintLauncher.cs` —— 服务侧根治（+615 行级别改动）

- **固定首选端口**：新增 `const int PreferredPort = 17817`。页面 origin 跨会话稳定，
  从根上消灭「换端口 ⇒ 老页面连不上」。
- **身份标识**：`const string PingTag = "WAVEPAINT-SERVICE"`；`/api/ping` 应答
  `PingTag + "\n" + listenPort + "\n" + buildStamp + "\n"`，并带 `Cache-Control: no-store`。
  前端据此区分「本应用」与「占了同一端口的第三方服务」，绝不误认。
- **`ivlRoot` 崩溃修复（根因 ④）**：
  - extract 分支末尾**统一** `ivlRoot = Path.Combine(root, "ivl")`（不再只在 cache 分支赋值）；
  - 新增 `EnsureIvlReady(out string error)` 二次兜底；
  - **每轮仿真前**也调一次 `EnsureIvlReady`：iverilog 不在了就从内嵌 `ivl.zip` 现补，
    补不了返回可读中文错误（而不是静默 500）。
- **协议重拉（关键交互设计）**：
  - `ParsePortArg(uri)` 手写解析 `wavepaint://start?port=49234`；
  - `MainCore` 区分 `protocolLaunch` + `requestedPort`；
    `StartServer(requestedPort > 0 ? requestedPort : PreferredPort)`；
  - 单实例互斥：已有活实例 → `OpenExistingInstance()`；互斥被占但探不到活服务 →
    记 `TAKE-OVER` 后继续自起（避免死进程永久霸占单实例锁）；
  - 启动失败 → 换新端口重试一次（兜底，不影响正常路径）。
- **发现与自检**：`PublishDiscovery()`（端口文件）、`SelfCheck()`、`HttpGetPing(Proxy = null)`、
  `IsSameOrigin()`（本实例端口 + 任意回环都放行，让跨端口重连成为可能）。
- **窗口探测改为真值**：`FindWaveWindowHandles()` 枚举全部可见顶层窗口（与
  `CloseLegacyWindows()` 同源，真值来源唯一）；`HasWindow()` 探测失败时**返回 true 保守**；
  退出条件收紧为 `everSeen && windowProbeOk && gone > 24 && idle > 90000`。

### B. `js/core/service-guard.js` —— 新增页面侧守卫（192 行，普通 script）

加载顺序：`heartbeat.js` 之后、`ui-bridge.js` 之前（ui-bridge 是 ESM/defer 语义，普通
script 先执行，保证 `window.WPServiceGuard` 在 ui-bridge 用到时已就绪）。

- `pingOrigin(origin, timeoutMs)`：带 `PING_TAG` 校验的判活；返回 `{origin, port, version}`。
- `ping()`：同源探活（页面自己就是服务端提供的，命中率最高）。
- `discover()`：同源 + `http://127.0.0.1:17817` 两个候选依次探测。
- `waitForService(timeoutMs, intervalMs)`：轮询等待上线。
- `relaunch()`：用 **隐藏 iframe** 加载 `wavepaint://start?port=<本页端口>`；
  **不是顶层跳转** —— 协议未注册时顶层跳转会顶掉当前页面，用户未保存的画布内容就没了。
- `recover(options)` → `{ state: 'alive' | 'restarted' | 'elsewhere' | 'dead', origin, port }`：
  - `alive` 服务就在本页 origin（刚才只是抖动）；
  - `restarted` 刚把 exe 拉起来且同源，可直接重试；
  - `elsewhere` 服务在**另一个**回环端口 → **只上报，不自动跳转**（本应用没有自动保存，
    跳转 = 丢未保存内容）；由调用方把 `simApiBase` 指过去；
  - `dead` 确实起不来 → 调用方给手动指引。

> ⚠ `relaunch` 能生效的前提：浏览器允许「外部协议」。协议 URL 由隐藏 iframe 加载时，
> 即使**没有用户手势**也能拉起 exe（实测，见验证 §3）；但首次浏览器会弹一次
> 「是否允许打开 wavepaint:」确认框，用户勾选「始终允许」后无感。

### C. `index.html`

- `heartbeat.js` 之后插入 `<script src="js/core/service-guard.js"></script>`（附注释说明
  为什么必须早于 ui-bridge、为什么是普通 script）。
- `#sim-status` 之后新增 `<button id="sim-recover" type="button" style="display:none;…">启动本地仿真服务</button>`
  —— 服务彻底死亡时的手动自愈入口（默认隐藏，由 ui-bridge 在需要时亮出）。

### D. `js/sim/ui-bridge.js` —— 页面侧自愈接线（+142 行）

- 新增 `let simApiBase = ""` + `simApiUrl(path)`：自愈到「另一个回环端口」时把 `/api/sim`
  指过去（服务端对回环 origin 放行 CORS，`text/plain` 属简单请求不触发预检）。
- 探活拆两层：
  - `probePlainPing(timeoutMs)`：裸 `GET /api/ping` 兜底（dev-server / 旧 exe 只回 `OK`）；
  - `probeServerAlive(timeoutMs)`：`tagged || plain || relocated` 三者任一为真即算在线
    （`relocated` 用于已经切到别的 origin 后不再误判离线、反复触发自愈）。
- `recoverService(options)` / `showRecoverHint()` / `showRecoverPending()`：
  - `showRecoverPending()` 是本轮新增 —— 自动恢复**进行中**先把手动按钮亮出来
    （用户可以立刻点，不必干等），但文案**不说「失败」**；
  - 旧实现在 catch 里先弹「自动恢复失败」再继续尝试自动拉起，字样一闪而过很误导，已替换。
- `/api/sim` 失败 catch 分支重写为三段自愈：
  1. 服务在线 → 只给准确文案（iverilog 超时 / 请求被丢弃），直接 return（行为不变）；
  2. 不在线 → ① `recoverService({ allowRelaunch: false })` 快速发现（~1s，不重拉）：
     `alive`/`elsewhere` → 设好 `simApiBase` → 自动重试一次（`simAutoRetried` 限一次）；
  3. 进程真死 → ② `showRecoverPending()` 亮手动入口 → `recoverService({ allowRelaunch: true, waitMs: 15000 })`
     → 成功自动重试；失败 → `showRecoverHint()` 保留手动按钮 + 中文指引。
- `#sim-recover` 点击：`recoverService({ allowRelaunch: true, waitMs: 20000 })` → 成功即重试仿真；
  `#sim-run` 点击复位 `simAutoRetried` 并收起按钮；仿真成功也收起按钮。

### E. `tools/dev-server.mjs`

- `/api/ping` 由 `res.end('OK')` 改为 `'WAVEPAINT-SERVICE\n' + port + '\ndev-server\n'` +
  `Cache-Control: no-store` —— 与 exe 身份标识格式对齐；否则前端身份校验会把
  dev-server 判成「离线」，本地开发时自愈逻辑会误触发。

---

## 三、验证结果（全部通过，本轮复跑确认）

| 项 | 结果 |
|---|---|
| `node --check` service-guard / ui-bridge / dev-server | 全部 exit 0 |
| `node tools/regression.mjs` | **62/62 通过** |
| `node tools/e2e-sim.mjs` | **失败 0 项** |
| `node tools/e2e-rtl.mjs` | **16/16 通过** |
| `node tools/e2e-ui.mjs` | **73/73 通过**（真实 Edge，无页面运行时错误） |
| `node tools/exe-smoke.mjs` | 全局 core/wpf/doc/canvas/汉化 全 object、弹窗汉化正常、**异常：无** |
| `.e2e-tmp/verify-recovery2.mjs`（本轮专门写的自愈探针） | **17/17 通过** |
| `.e2e-tmp/probe-protocol.mjs`（真实 Edge + 预置协议允许表） | T1 隐藏 iframe ✅ 拉起 exe；T2 `<a target=_blank>`、T3 `window.open`、T4 顶层跳转 ❌ 被 `Not allowed because a user gesture is required` 拦下 |

`verify-recovery2.mjs` 覆盖（CDP 端口 9541，headless Edge，profile `edge-recover-*`）：

- **A 组**：A1 ping 返回身份标识 / A2 真 VCD 编译（985 B、238 ms）；
- **B 组**：B0 页面已加载守卫 / B1 ping 命中 17817 / B2 `recover()` → `alive`；
- **F 组**：F1 `relaunch()` 生成 `wavepaint://start?port=17817`（URL 口径正确）；
- **C 组**：C1 exe 消失 / C2 ping → null / C3 `recover(不重拉)` → `dead` /
  **C4 `relaunch()` 不顶掉当前页面**；
- **D 组**：**D1 点 `#sim-run`（服务已死）→ 自愈 → 「仿真完成」** /
  D1b 先亮手动入口（`pending=true`、`hint=false`）/ D2 exe 重拉（pid 16856）/
  **D3 回到同一端口 17817** / D4 身份标识仍在 / D5 自愈后仿真 200 / 172 ms / 985 B /
  **D6 页面全程未跳转**。

> 探针注意：headless 不会真调外部协议，脚本在点击后 3 s 用
> `powershell Start-Process 'wavepaint://start?port=<port>'` 模拟 OS 协议回调；
> 「iframe 真能拉起 exe」由 `probe-protocol.mjs`（真实 Edge）单独证明。

exe 特征串核验（C8，`rg -a -c`）：`WAVEPAINT-SERVICE`=1、`wavepaint:`=11、
`WPServiceGuard`=5、`sim-recover`=2、`showRecoverPending`=2、`probePlainPing`=2、
`simApiBase`=8。

exe 已按 C1 重建：`WavePaintClean.exe` **21,881,344 B**、时间戳 **2026-09-10 20:05:06**、
`version.txt` = `v0.4.0 build 2026-09-10 20:05:06 72116e0`。

---

## 四、关键结论（交给下一位 AI）

1. **「点仿真无响应」今后应视为 bug**：正常路径必须是「点 → 服务在 → 仿真成功」。
   任何形态的「请重启应用」都只是兜底，不是答案。
2. **端口固定 17817 是刻意设计**，不是巧合：页面 origin 稳定 ⇒ 跨会话可重连、
   localStorage 稳定（顺带缓解 #81 Bug3 教程重跑）。改端口策略前先读
   `memory/06-PITFALLS.md` P29。
3. **恢复策略一律「改 `simApiBase` / 同端口重拉」，禁止自动整页跳转**：
   本应用**没有自动保存**，跳转 = 用户画布内容全丢。见 P30。
4. `relaunch()` **必须用隐藏 iframe**：顶层跳转 / `window.open` / `<a target=_blank>`
   都会被浏览器的用户手势策略拦下（实测），且顶层跳转在协议未注册时还会顶掉页面。
5. 换版本后首次启动走 extract 分支 —— 这是**每轮重建 exe 后都会遇到**的路径，
   任何「只在 cache 分支初始化」的写法都是定时炸弹。见 P28。

---

## 遗留问题 / 下一步

- **本专项无阻塞**。服务在线性闭环：进程死 / 换端口 / 端口被占 / 首启崩溃 四类死因都已处理。
- 主线未开工，顺序不变：**#86（A1~A4）→ #76（B1 符号索引/scope 映射 → B4 代码内点/选中
  变量加波形 = 唯一加信号主路径 → B3/B5）→ #87②**；UI 重构方案线（08 §2）并行。
- 未验证项（明确记录，避免后人误以为已验证）：**「浏览器首次弹『是否允许打开 wavepaint:』
  并勾选始终允许」这一步只有真实用户桌面能走完**，自动化（headless）无法覆盖；
  本轮用预置协议允许表 + 模拟 OS 回调等价替代。

## 交付提醒

- 重启应用、从唯一路径启动 `D:\Files\Code\波形\WavePaintClean.exe`，面板版本号应显示
  `v0.4.0 build 2026-09-10 20:05:06 72116e0`。
- 本批行为变化：服务掉线时页面会**自动重连 / 自动重新拉起服务并重试仿真**；
  自动恢复失败时右侧仿真面板会出现「启动本地仿真服务」按钮，点一下即可。
- **首次**使用该按钮（或任何自动重拉）时，浏览器会弹一次
  「是否允许此网站打开 wavepaint:」→ 请勾选「始终允许」；之后完全无感。这是浏览器
  外部协议安全策略，无法从应用侧绕过。

---

## 第十四轮：#86 A1~A4 全部落地 —— 实例→模块定义跳转 + 磁盘导入源码 + 源码集合随 `.wp` 存档（2026-09-10）

> 用户指令：「继续回到原来的任务，继续进行，按规划进行」→ 开工主线 **#86 A1~A4**
> （`09-HANDOFF.md` §3 写明的下一步）。本轮**只做 #86，不碰 #76**；服务自愈（第十三轮）
> 是独立专项，本轮回过头补齐了它的日志标题（见上）。

### 一、本轮目标

按 08 §1.1 的 A1~A4 拆解，把「代码↔波形交互 + 底层解析能力」的第一步做完：

1. **A1 底层解析/索引增强**：例化识别要覆盖参数化例化 / 命名/位置端口 / generate 内例化 /
   多实例同语句；补「模块定义候选」索引。
2. **A2 交互**：RTL 树实例行 → 跳**模块定义**行；「跳到例化点」保留为次入口。
3. **A3 交互**：磁盘多选导入 `.v/.sv` 源码 → `state.files`。
4. **A4**：源码集合随 `.wp` 工程存档/恢复（上一轮列为“待用户拍板”，本轮用户明确
   「按规划进行」= 拍板实施）。

### 二、改动清单（6 个代码文件）

**1. `js/sim/rtl-nav.js`（+311 行，A1）**

- 新增 `scanInstances(text, from, to, lineOf)`：**源码级实例扫描器**。字符流逐字扫，
  `matchParen` 处理平衡括号，支持「多实例同语句」`sub u1(a), u2(b), u3(c);`，必须以 `;`
  收尾才算一条例化；行号取自真实字符下标（不是 re-split 后的行号，避免错位）。
- 新增 `maskStrings(source)`：把**字符串内容**换成**等长空格**（保留引号与长度），供后续
  扫描不误判字符串里的“例化”。修了一个未闭合串少 1 的 bug：`inner = Math.max(0, scan - index - 1)`。
- 新增 `mergeInstances(scanned, fromEngine)`：合并扫描器结果与 engine 结果，key =
  `moduleName.toLowerCase()|instanceName`；**扫描器优先，engine 兜底**；engine 条目要过**两道闸**
  ——① `isReservedName` 排除门原语（`and`/`or`/`nand`…）；② 在**抹白后的文本**里必须真存在
  `<实例名> (`。最后按行号排序。
- 新增 `collectModuleDefs(nav)` / `findModuleDefCandidates` / `resolveModuleDef(defs,
  moduleName, preferFileIndex)`：模块定义候选索引；同名多处 →**同文件优先**，否则取第一处；
  精确命中 → 否则大小写模糊并标 `fuzzy`。
- **设计动机**：绕开 `sim/engine.js` 的 `matchSignalsToPorts`（C9 最易碎区），只读扫描，
  **一行都没改 engine**。

**2. `js/sim/rtl-panel.js`（A2）**

- `makeJumpRow(...)` 增第 6 参 `secondaryJump`：**左键 = `jump`，右键 `contextmenu` /
  Alt+左键 = `secondaryJump`**。
- 实例行 payload 扩为 `{fileIndex,line,moduleName,instanceName,name,kind}`；
  `kind === "instance"` → 跳模块定义；`kind === "instanceSite"` → 跳例化点。
- row 的 `title` 写明两种语义（左键跳定义 / 右键跳例化点）。

**3. `js/sim/ui-bridge.js`（+278 行，A2/A3/A4）**

- import 增 `collectModuleDefs, resolveModuleDef`。
- `gotoSource(fileIndex, line)`：跨文件先 `syncEditor()` → 切 `state.active` →
  `renderFileTabs()` → `jumpToEditorLine`（切 tab 后再跳，避免跳到旧文件）。
- `refreshStructureTrees()`：`buildRtlNav` → `collectModuleDefs`；
  `kind === "instance"` 时 `resolveModuleDef` 跳定义 + 状态栏「已定位到 module X 的定义（file:line）」；
  未命中 → 「未找到模块 X 的源码定义（可能是黑盒或外部 IP），已定位到例化点第 N 行。」；
  `instanceSite` → 「已定位到 X 的例化点（第 N 行）。」
- **A3**：`SOURCE_FILE_ACCEPT`、`readSourceFilesViaInput()`、`pickSourceFilesFromDisk()`
  （优先 `showOpenFilePicker`，隐藏 `<input type=file>` 兜底）、`uniqueSourceFileName()`
  （同名去重）、`importSourceFiles()`。
- **A4**：`archiveBridgeInstalled`、`indentBlock()`、`injectArchiveSourceFiles(json, files,
  activeIndex)`（**文本拼接**在末尾 `}` 前注入 `sourceFiles`/`activeSourceIndex`，空文档 `{}`
  不多逗号）、`flushEditorIntoSourceFiles()`（**故意不用 `syncEditor()`**，避免清掉
  design/outputs/TB）、`archiveSourceFiles()`、`installProjectArchiveBridge()`（包裹
  `window.buildDocumentJson` / `window.loadFromFileContent`，失败 `console.warn` 静默降级）、
  `applyArchivedSourceFiles(text)`（带 `sourceFiles` → 整体替换；旧工程 → 保持现状；
  `refs.sourceFiles` 未就绪则只写 `state.files`）、`resetSourceFiles()`、`setSourceFiles(files)`。
- 模块末尾：`installProjectArchiveBridge();`（**必须尽早**，分享链接 `#d=` 载入发生在核心
  初始化期）+ `window.__wpsim = { get sourceFiles, get active, get archiveInstalled,
  setSourceFiles, resetSourceFiles, importSourceFiles }`（探针入口）。

**4. `index.html`（A3）**：`.source-actions` 在 `sim-addfile` 后新增
`<button id="sim-import" title="从磁盘导入 .v/.sv 源码文件（可多选）">导入源码</button>`。

**5. `js/editor/file-menu.js`（A4）**：`onNew()` 里 `resetDocumentInPlace()` 后加
`try { window.__wpsim && window.__wpsim.resetSourceFiles(); } catch {}`。

**6. 测试**：`tools/regression.mjs` rtl-nav group 新增 6 条断言；`tools/e2e-rtl.mjs`
新增 E1~E6（23/23）。

### 三、验证结果（本轮实跑，全绿）

| 命令 | 结果 |
|---|---|
| `node --check` 4 个 js + `tools/e2e-rtl.mjs` | ✅ |
| `node tools/regression.mjs` | ✅ **68/68**（基线 62 + 新增 6） |
| `node tools/e2e-rtl.mjs`（真实 Edge） | ✅ **23/23**（基线 16 + E1~E6） |
| `node tools/e2e-sim.mjs` | ✅ 失败 0 项 |
| `node tools/probe-param.mjs` | ✅ 全部通过 |
| `node tools/e2e-ui.mjs`（真实 Edge） | ✅ **73/73** |
| `.\build.ps1` | ✅ `csc exit: 0` |
| C8 特征串 `rg -a -c` | ✅ `scanInstances`=2 / `resolveModuleDef`=3 / `collectModuleDefs`=3 / `sim-import`=2 / `archiveInstalled`=1 / `__wpsim`=2 / `setSourceFiles`=5 / `injectArchiveSourceFiles`=2 |
| 真 exe 冒烟（C15 一条命令） | ✅ 端口 17817；`{"core":"object","wpf":"object","doc":"object","canvas":true,"zh":true}`；弹窗「添加时钟信号 / 时钟名称：/ 确定」；异常无 |

**E 段新增断言（e2e-rtl）实测输出**：

- E1 `{"has":true,"installed":true,"files":1,"hasSetter":true}` —— `.wp` 存档桥已安装、`__wpsim` 入口就绪。
- E2 `{"label":"u_sub : sub  L3","status":"已定位到 module sub 的定义（sub.v:1）。","active":1,"chip":"sub.v"}` —— 左键实例行跨文件跳定义。
- E3 `{"status":"未找到模块 ext_ip 的源码定义（可能是黑盒或外部 IP），已定位到例化点第 3 行。"}` —— 黑盒中文提示。
- E4 `{"status":"已定位到 sub 的例化点（第 3 行）。","active":0}` —— 右键/Alt 跳例化点。
- E5 `{"savedHas":true,"savedCount":2,"savedActive":0,"savedFirstHasText":true,"loadOk":true,"loadedCount":2,"loadedNames":"loaded_a.sv,loaded_b.v","loadedActive":1}` —— 源码集合 `.wp` 往返（2 文件 + active 索引 + 文本完整）。
- E6 `{"exists":true,"label":"导入源码","hasClick":true}` —— 侧栏按钮存在且可点。

exe 已按 C1 重建：`WavePaintClean.exe` **21,910,016 B**、时间戳 **2026-09-10 20:36:16**、
`version.txt` = `v0.4.0 build 2026-09-10 20:36:16 327acf7`。

### 四、关键结论（交给下一位 AI）

1. **实例识别走「自写扫描器」，不要改 `sim/engine.js`**：engine 的懒匹配会吞掉多实例同语句、
   还会被字符串里的假例化骗到（见 06 P31）。`scanInstances` + `maskStrings` 是纯只读方案，
   继续沿用它扩展即可。
2. **实例行有两种语义**：左键 = 跳 module **定义**，右键 / Alt+左键 = 跳**例化点**。
   改 `makeJumpRow` 时别把 `secondaryJump` 参数丢了。
3. **源码集合的存档口径**：`.wp` 里以 `sourceFiles` + `activeSourceIndex` 形式追加；
   写入用**文本拼接注入**（保护空文档 `{}` 不多逗号）；读入对旧工程必须**向后兼容**
   （没有 `sourceFiles` 就保持现状、不报错）。桥必须在核心初始化前安装（`#d=` 分享链接
   载入发生在初始化期）。
4. **`flushEditorIntoSourceFiles` 故意不走 `syncEditor()`**：`syncEditor()` 会顺带清
   design/outputs/TB，存档时用它会**误删**这些字段。
5. **A4 是 B5 的现成入口**：#76 B5（信号组/观察行入 `.wp`）可直接复用本轮的
   `installProjectArchiveBridge` / `injectArchiveSourceFiles` 存档桥，不要另起一套。

### 五、遗留问题 / 下一步

- **本轮无阻塞**。#86 全部收官；服务在线性（第十三轮）已闭环。
- 主线下一项 = **#76 B1（模块体内符号索引 + VCD scope 映射）** → B4（代码内点/选中变量 →
  加波形 = 唯一加信号主路径，复用 #85 `state.simWatches` 链路）→ B3/B5 → **#87②**；
  侧栏/整体 UI 重构（08 §2）为并行方案线，先出方案给用户 review。
- 开工 #76 B1 时**优先复用**本轮的 `maskStrings`（安全抹白）与 `collectModuleDefs`（定义索引），
  以及 `state.files` / `sourceFiles` 存档桥。

## 交付提醒（第十四轮）

- 重启应用、从唯一路径启动 `D:\Files\Code\波形\WavePaintClean.exe`；面板版本号应显示
  `v0.4.0 build 2026-09-10 20:36:16 327acf7`（本批含第十四轮 #86 A1~A4 + 第十三轮服务自愈）。
- 本批新增可见功能：RTL 树实例行**左键跳模块定义、右键跳例化点**；侧栏新增**「导入源码」**
  按钮（可多选 `.v/.sv`）；**源码文件集合随 `.wp` 工程一起保存/恢复**（旧工程打开兼容，
  只是没有源码集合）。
- 服务掉线自愈行为同第十三轮：自动重连/自动重新拉起服务并重试仿真；**首次**会弹一次
  「是否允许此网站打开 wavepaint:」→ 勾选「始终允许」后无感。

---

## 第十五轮：#76 B1 数据层落地 —— 模块体内符号索引 + 例化路径 + 符号→VCD 全路径映射（2026-09-10）

### 一、本轮目标

用户指令：「OK, 继续回到原来的任务，继续进行，按规划进行」→ 按 08 §1.2 开工近期主线
第二项 **#76**，本轮完成**第一顺位 B1（底层解析底座）**。B1 是 B4（代码内点变量 → 加波形 =
唯一加信号主路径）与 B3（代码↔树双向跳转）的共同底座，因此本轮只做**数据层**，不碰 UI
交互（B4 才接线）。

### 二、改动清单（5 个文件）

**1. `js/sim/rtl-nav.js`（核心新增，仅新增、不动既有 `buildRtlNav` 语义）**

- 数据声明关键字表 `DATA_DECL_KEYWORDS`（wire/reg/logic/bit/integer/genvar/tri/time/
  real/shortint/… 共 26 个）、隐式位宽表 `IMPLICIT_WIDTHS`（integer/int=32、time/longint=64、
  shortint=16、byte=8、real/realtime=64、shortreal=32）、类型词表 `DECL_TYPE_WORDS`、
  扫描关键字表 `DECL_SCAN_KEYWORDS`（数据关键字 + input/output/inout + parameter/localparam）。
- 内部工具：`blankInnerScopes(text)`（把 `function…endfunction` / `task…endtask` /
  `specify…endspecify` / `table…endtable` **整体抹成等长空格**，保留换行 → 函数/任务的
  形参与局部变量**不进符号表**）、`declRangeInfo(rangeText)`（`[7:0]` → `{msb,lsb,width,range}`，
  非静态可求值的参数化位宽 width=0 但保留 range 原文）、`findStatementEnd`（深度 0 的 `;`）、
  `splitDeclItems`（深度 0 逗号切分）、`declItemInfo`（单项 → `{name,ownRange,value}`）、
  `readDeclStatement`（吞类型词 → 取位宽 → 取标识符列表；`parameter type T = logic;` 直接放弃）。
- **导出 `scanModuleSymbols(content, blanked, from, to)`**：在 `blankInnerScopes(maskStrings(blanked))`
  上跑关键字扫描，产出 `{name, kind:'port'|'parameter'|'localparam'|'reg'|'wire'|'logic'|
  'integer'|'genvar'|…, direction, width, msb, lsb, range, value, line}`；行号用
  `lineNumberOf(原文, 真实下标)` ⇒ 注释/字符串抹白**不影响行号**。
- 内部 `mergeSymbols(groups)`：同名同行去重（key=`name|line`）+ 按行号排序（稳定 sort 保
  同行内优先级）。
- **导出 `buildInstancePaths(nav, options)`** → `{topName, topNames, paths:[{moduleName,file,
  fileIndex,line,path,depth}]}`；`path` 是**不含信号名**的作用域前缀（`tb.dut`、`tb.dut.u_a`）；
  options = `{topName, tbScope='tb', topInstance='dut', maxDepth=32}`；顶层未指定时自动判
  （没被任何模块例化过的模块）；带**例化环保护**（`stack` 按 defKey + `seen` 按 `key|path`）。
- **导出 `buildSymbolIndex(nav, options)`** → `{topName, topNames, modules:[…], symbols:[已展平，
  带 moduleName/file/fileIndex], instancePaths}`；
  `moduleAtLine(index, fileIndex, line)`（取包含该行的**最外层**模块，越界 null）；
  `findSymbols(index, name, options)`（`{moduleName,fileIndex,line}` 逐级收窄，
  **收窄后为空则忽略该条件**；大小写不一致标 `fuzzy`）；
  `resolveSymbolVcdPaths(index, name, options)` → `{paths, hits, fuzzy}`。
- `buildRtlNav` 每个模块结果新增 `endLine` 与 `symbols`（= `mergeSymbols([体内声明, 端口,
  头部参数, 实例名])`，**体内声明在前**：唯一重叠是体内 localparam，体内扫描的关键字更准）。
- 注释里写明三条**有意为之**的边界：不索引 assign 隐式 net、不索引自定义类型变量、
  不索引 named begin 内局部变量。

**2. `js/sim/vcd-index.js`（+1 导出）**

- 新增 `findVcdPathsByName(parsed, name)`：按信号名**末段**在 `parsed.signals` 里取全部全路径
  （scope 点分 + name），去重、保序、空入参安全。用途 = 代码里的符号没能在模块体符号索引
  命中时（隐式 net / TB 本地信号 / 自定义类型变量）做**兜底候选**。

**3. `js/sim/ui-bridge.js`（接线 + 探针）**

- import 扩为 `buildRtlNav, buildSymbolIndex, collectModuleDefs, resolveModuleDef,
  resolveSymbolVcdPaths` + `buildVcdHierarchy, findVcdPathsByName`。
- 新增 `currentSymbolIndex()`（`refreshVcdTree` 之后）：`buildSymbolIndex(buildRtlNav(state.files),
  { topName: state.selectedTop || state.design?.topModule?.name || "" })`，**纯计算、无副作用、不碰画布**。
- `window.__wpsim` 新增：`get symbolIndex()`（返回 `{topName, topNames, moduleCount,
  symbolCount, instancePaths}`）、`symbolsOf(fileIndex)`、`symbolVcdPaths(name, options)`、
  `vcdPathsByName(name)`。

**4. `tools/regression.mjs`（+7 条断言，62→…→75）**

- rtl-nav 组末尾新增 6 条 B1 断言（`B1_TOP_SRC` / `B1_SUB_SRC` / `b1Files()` 三个 fixture）：
  ① 体内声明索引（wire mid L6/w8、P parameter L7/value 3、Q **localparam**、u_a instance→sub、
  符号按行有序）；② ANSI/非 ANSI 端口 + `output q`/`reg q` 同名两条 + `output wire signed
  [7:0] b` 位宽；③ function 内局部变量/形参**不进表** + integer=32 + 注释/字符串里的 wire
  不索引；④ 例化路径（`tb.dut`、`tb.dut.u_a`、`tb.dut.u_b`、自动判顶层、**例化环不挂死**）；
  ⑤ 符号→VCD 候选（`din` → 3 条；收窄 module/line；`MID` fuzzy；单模块 → `tb.dut.acc`）；
  ⑥ `moduleAtLine`。
- vcd-index 组末尾新增 1 条：`findVcdPathsByName` 两条同名 q → `["tb.dut.q","tb.dut.u_a.q"]`，
  空名/null 入参安全。

**5. `tools/e2e-rtl.mjs`（+9 条 F 段断言，23→32）**

- F 段自带两文件 fixture（`counter.sv` 例化 `sub.v`，**两个模块里都有 q** → 跨模块同名），
  `setSourceFiles` 后**跑一次真实仿真**，再用 `__wpsim` 探针核对代码符号 → 真实 VCD 全路径。

### 三、验证结果（本轮实跑，全绿）

| 套件 | 结果 |
|---|---|
| `node --check`（rtl-nav / vcd-index / ui-bridge / e2e-rtl） | 全过 |
| `node tools/regression.mjs` | **75/75**（第十四轮 68 + B1 新增 7 条） |
| `node tools/e2e-rtl.mjs` | **32/32**（真实 Edge；第十四轮 23 + 新增 F1~F9） |
| `node tools/e2e-sim.mjs` | 失败 0 项 |
| `node tools/probe-param.mjs` | 全部通过 |
| `node tools/e2e-ui.mjs` | **73/73**（真实 Edge） |
| `node tools/exe-smoke.mjs` | 真 exe 冒烟通过（端口 17817、core/wpf/doc/canvas/汉化全在、无异常） |

**F 段断言实测输出（真实 VCD 核对）**：`symbolIndex` = `topName:counter / moduleCount:2 /
symbolCount:10 / paths:["tb.dut","tb.dut.u_sub"]`；点 `counter.sv` L7 的 reg q →
`["tb.dut.q"]`；点 L5 端口 q → `["tb.dut.q"]`；点 `sub.v` L2 的 reg q →
`["tb.dut.u_sub.q"]`（跨模块同名不串味）；只给名字 `q` → 两条候选；名字兜底
`vcdPathsByName('q')` = `["tb.q","tb.dut.q","tb.dut.u_sub.q"]`（**覆盖**符号侧全部候选）；
`wire acc`(L8) → `tb.dut.acc`；不存在符号 → 空候选；越界 fileIndex/line → 因「收窄为空则忽略」
回退到全部同名候选；**符号声明位宽 4 == VCD 实际位宽 4（vector）**。

### 四、关键结论（交给下一位 AI）

- **B1 只是数据层**：`window.__wpsim.symbolIndex / symbolsOf / symbolVcdPaths / vcdPathsByName`
  是**探针入口**，不是交互。**B4 才把这些接到代码区的点选/快捷键上**。别在本轮基础上
  直接改 RTL 树（RTL 树仍是纯层级浏览，B2 已撤销）。
- **`findSymbols` 的收窄契约**：`{moduleName,fileIndex,line}` 能收窄才收窄，
  **收窄后为空则忽略该条件**（不会因为行号不精确就返回空）—— 这是给 B4 用的容错，
  有 F8 断言守着，改之前先看 e2e-rtl F8。
- **行号真相源不变**：符号行号一律按**原始文件文本**算，函数/任务体先被 `blankInnerScopes`
  抹成**等长空白**再扫（等长是硬要求，见 06 P31/P32）。不要为了省事改成正则直接扫原文。
- **边界（有意为之，不是 bug）**：不索引 `assign` 隐式 net、不索引自定义类型变量、
  不索引 named begin 内的局部变量 → 这三类由 `findVcdPathsByName` 在 VCD 侧兜底。
- **未改 `sim/engine.js` 一行**（C9 仍然守住）：本轮全程只读调用 engine 的产物。

### 五、遗留问题 / 下一步

- **本轮无阻塞**。B1 数据层闭环（regression 75/75、e2e-rtl 32/32 全绿）。
- 下一步 = **#76 B4**：代码区点/选中变量 → 加波形（唯一加信号主路径，仿 Ctrl+W「中追」）。
  复用链路：`__wpsim.symbolVcdPaths` / `findSymbols`（候选）→ `state.simWatches` +
  `pickVcdSignalIntoWave`（#85）→ `scrollWaveToWatchPath`；候选多于 1 条时只对**代码操作**
  弹选择器（**不引入单独“信号层次选择框”**）。同时按用户口径统一「运行仿真后不再全量自动
  加信号」。
- 其后 B3（代码 ↔ 树双向跳转）→ B5（信号组入 `.wp`）→ **#87②**（模块全接口 Ctrl+4）。
- 侧栏 / 整体 UI 重构（08 §2）仍为并行方案线，先出方案给用户 review。

## 交付提醒（第十五轮）

- 重启应用、从唯一路径启动 `D:\Files\Code\波形\WavePaintClean.exe`；面板版本号应显示
  `v0.4.0 build 2026-09-10 21:06:52 db7a98f`。
- **本批对用户界面无可见变化**：B1 是纯数据层（符号索引 / 例化路径 / 符号→VCD 全路径映射），
  面向下一步「代码里直接点变量加波形（B4）」。可感知的新行为要等 B4。

---

## 第十六轮：#76 B4 交互落地 —— 代码内点/选中变量 → 加波形（唯一加信号主路径）（2026-09-10）

### 一、本轮目标

用户指令：「按照规划继续进行」→ 按 08 §1.2 实施 **#76 B4**。B4 是第十二轮澄清后定下的
**唯一加信号主路径**（仿 Verdi nWave「中追」/ Ctrl+W），本轮同时**收敛**了「运行仿真后把
所有可看变量全量灌进画布」的旧做法（用户明确不要，属 #83③c 遗留）。

### 二、改动清单（6 个文件）

**1. `js/sim/rtl-panel.js`（取词纯函数 + 编辑器触发面）**

- 新增导出纯函数 **`symbolNameAt(text, from, to)`**：在给定文本与位置（或选区）上取「用户
  想加进波形的那一个信号名」。内部：`VERILOG_IDENT` / `IDENT_SCAN` / `IDENT_CHAR`、
  `BITSEL_LOOKBACK`（回看是否处在 `q[3:0]` 这类位选之后）、`VERILOG_KEYWORDS`（~110 词，
  排除 `always`/`begin`/`end`/`if` 等）、`baseSymbolName()`（剥位选与下标）、
  `firstSymbolIn()`（`a.b.c` 取末段）、`isIdentAdjacent()`。
- `installCodeEditor(...)` 增第 5 个参数 **`onAddSymbol`**；内部 `readContext()` 产出
  `{name, line, selection, exact, source}`（`exact` = 是否精确命中完整标识符；
  `source` = `'cm'` | `'textarea'`）；返回对象新增 `getContext()` 与 `focus()`。
- **触发面（三入口进同一 `onAddSymbol`）**：① `dblclick`（冒泡，要求 `exact`）；② `contextmenu`
  （`preventDefault()`）；③ `keydown` **捕获阶段 `Ctrl+Alt+W`**。**刻意不用 `Ctrl+W`**
  （Edge `--app` 吞它当关窗）。
- **未改** `tools/cm6-entry.js` / `lib/codemirror.bundle.js`：触发挂在**宿主 DOM** 上，
  **不需重跑 esbuild**。

**2. `js/sim/ui-bridge.js`（接线 + 浮层选择器 + 收敛全量灌信号）**

- 新增 `onAddSymbol: (context) => addSymbolFromCode(context)`。
- 新增 **`addSymbolFromCode(context)`**：`currentSymbolIndex()` → `moduleAtLine`（行 → 模块）→
  `resolveSymbolVcdPaths(index, name, {moduleName, fileIndex, line})`；**仅当候选为空**才用
  `findVcdPathsByName` 兜底（精确与兜底**不做并集**）；唯一候选 → `pickVcdSignalIntoWave(path)`；
  多候选 → `showSymbolPicker(候选)`；未命中 → 中文提示。
- 新增单例浮层 `symbolPicker` / `showSymbolPicker()` / `closeSymbolPicker()`；DOM/CSS 是
  **代码区旁的轻量浮层** `.sim-symbol-picker`（**不是**单独「信号层次选择框」，不放侧栏、
  不放 RTL 树）。
- **`replaceInjectedOutputs` 改名 `syncSimRows()`**，且**不再把 `outputs` 全量灌进画布**。
- `window.__wpsim` 新增探针：`addSymbolFromCode` / `codeContext` / `symbolPickerOptions` /
  `clickSymbolPickerOption` / `closeSymbolPicker` / `nativeValuesOfOutput` / `outputs` / `simWatches`。

**3. `index.html`**：新增 `.sim-symbol-picker` / `-title` / `-item` 系列 CSS（+39 行，插在
`.sim-status` 之后、`#sim-panel` 之前）。

**4. `tools/regression.mjs`（+2 条，75→77）**：新增
`group("rtl-panel.js（#76 B4 代码取词纯函数）")`，断言 `symbolNameAt` 在「标识符中间 /
位选 `q[3:0]` / `a.b.c` / 关键字 / 空白处」的取词行为。

**5. `tools/e2e-sim.mjs`**：导出清单扩 `syncSimRows` 等；断言改为「**不自动灌信号**」+
「登记真实 VCD 路径后，观察行与原生信号**等长同子步**」。

**6. `tools/e2e-rtl.mjs`（+7 条 G 段，32→39）**：G 段 fixture = `counter.sv` 例化两次 `sub`
（`u_a` / `u_b`），两文件里都有 `reg [3:0] q`（跨模块同名，用于验证「多候选」）。

### 三、验证结果（本轮实跑，全绿）

| 套件 | 结果 |
|---|---|
| `node tools/regression.mjs` | **77/77** |
| `node tools/e2e-rtl.mjs` | **39/39**（G 段 7 项全 PASS） |
| `node tools/e2e-sim.mjs` | 失败 0 项（含「仿真后未自动灌信号」断言 ✅） |
| `node tools/e2e-ui.mjs` | **73/73**（真实 Edge） |
| `node tools/probe-param.mjs` | 全部通过 |
| `node tools/exe-smoke.mjs` | 真 exe 冒烟通过（端口 17817、core/wpf/doc/canvas/汉化全在、无异常） |

**G 段实测输出（真实浏览器 + 真实 VCD）**：G1 `autoInjected:0 / autoWatch:0`（仿真后画布
**没有**被自动灌信号）；G2 点 `counter.sv` 的 `q`（`{name:'q',fileIndex:0,line:7}`）→
`tb.dut.q`、`uniquePicker:null`（唯一候选直接加，不弹选择器）；G3 点 `sub.v` 的 `q`
（`{fileIndex:1,line:2}`）→ `["tb.dut.u_a.q","tb.dut.u_b.q"]` 两条候选且**先不加入**；
G4 `clickSymbolPickerOption` → 加入 `tb.dut.u_b.q` 且 `pickerClosed:true`；G5 未知符号 →
`unknownDelta:0`（零副作用）+ 中文提示；G6 右键 / `Ctrl+Alt+W` 派发到 `#verilog-cm-host`
走同一条链路；G7 `codeContext.source === 'cm'`。

**exe**：已按 C1 重建 —— `WavePaintClean.exe` **21,947,904 B**、时间戳 **2026-09-10 21:33:06**、
`version.txt` = `v0.4.0 build 2026-09-10 21:33:05 ebce2b5`。C8 特征串（字节计数）：
`symbolNameAt`=4、`addSymbolFromCode`=5、`sim-symbol-picker`=11、`syncSimRows`=3、
`resolveSymbolVcdPaths`=6（全部 > 0）。

### 四、关键结论（交给下一位 AI）

- **B4 是唯一加信号主路径**：用户明确「通过代码直接下信号，而不是 RTL tree」。RTL 树只做代码
  层级浏览（文件→模块→实例，点击跳源码），**不要**给它加回端口/参数/信号行，也不要恢复
  「点行加波形」。
- **三条硬口径（违反即回退用户明确否掉的东西）**：① 快捷键 = **捕获阶段 `Ctrl+Alt+W`**，
  不是 `Ctrl+W`（Edge `--app` 吞 `Ctrl+W` 当关窗，页面收不到 —— 见 06 P33）；② 精确候选与
  名称兜底**不取并集**（精确命中就不看兜底，并集反而更差，见 06 P33）；③ 多候选选择器
  **只能挂在代码区旁**，严禁另建「信号层次选择框」（用户明确不想要）。
- **不要恢复「仿真后全量自动灌信号」**：`syncSimRows` 只同步由真实 VCD 路径登记的观察行
  （`state.simWatches`），不再推 `outputs`。
- **B3 的地基已就位**：`rtl-panel` 的 `getContext()` 返回 `{name,line,selection,exact,source}`，
  配 B1 的 `moduleAtLine`/`findSymbols` 即可定位「哪个模块、哪一行、什么符号」——
  **不要另写一套解析**。
- **未改 `sim/engine.js` 一行**（C9 仍然守住）：本轮全程只读调用 engine 的产物。

### 五、遗留问题 / 下一步

- **本轮无阻塞**。B4 闭环（regression 77/77、e2e-rtl 39/39 全绿）。
- 下一步 = **#76 B3：代码 ↔ 树双向跳转**（补「代码符号/行 → 反向高亮/定位 RTL 树与 VCD 树
  节点」，已有「树 → 代码」= #86 A2 双语义），用 B4 的 `getContext()` + B1 的
  `moduleAtLine`/`findSymbols`。
- 其后 B5（信号组/观察行随 `.wp` 存档）→ **#87②**（模块全接口 Ctrl+4）。
- 侧栏 / 整体 UI 重构（08 §2）仍为并行方案线，先出方案给用户 review，不直接开工。

## 交付提醒（第十六轮）

- 重启应用、从唯一路径启动 `D:\Files\Code\波形\WavePaintClean.exe`；面板版本号应显示
  `v0.4.0 build 2026-09-10 21:33:05 ebce2b5`。
- **本批两条用户可见变化**：① 代码里**双击变量名 / 右键 / 按 `Ctrl+Alt+W`** 就能把该信号
  加进波形（多实例同名时在代码区旁弹一个轻量选择器让用户挑）；② 点「运行仿真」后**不再把
  所有可看变量全量灌进画布**（只保留用户自己加的观察行）。
- 若此前桌面/任务栏堆积了多个「WavePaint 波形编辑」窗口，先全部关掉，再确认没有
  `WavePaintClean.exe` 旧进程残留后重新启动一次（#82 起 launcher 会自动收旧窗）。
