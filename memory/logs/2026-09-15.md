# 2026-09-15 WavePaint 工作日志

---

## 第五十四轮（2026-09-15 · 源码搜索根自动补齐）

- **需求**：自动加信号**不再手动选文件夹**；文件夹配置**可更改**（一般与 top 同级；`std` 可能独立目录）；
  默认搜索 = top 所在目录**及同目录并往上 3 层**。另：查清 Verdi 免 filelist 打开 top 的方式。
- **调研结论**：Verdi = `-y <dir>` + `+libext+.v+.sv`（只编「文件名 = 被引用模块名」的文件，新引用再触发搜索，
  多轮收敛）。本项目已对齐：多根 + 文件名约定 + 内容兜底 + `DEP_RESOLVE_MAX_ROUNDS=3`。
  ⚠ Edge/Chromium 的 `FileSystemDirectoryHandle` **无 `getParent()`** ⇒ 「往上 3 层」只能靠**本地服务读盘**。
- **实现**：`WavePaintLauncher.cs` 新增 `api/fs`（`probe|list|index|read`，白名单扩展名、8MB 单文件、2000 文件索引）
  + `api/pick`（WinForms 原生对话框）；安全闸门 `FsCallerAllowed`（loopback + `X-WavePaint-Fs: 1` + `Sec-Fetch-Site: same-origin`）。
  前端 `ui-bridge.js` 新增 `fsApi*` / `depSearchRoots`（`wavepaint.sourceRoots.v1`）/ `autoFillDependencies` /
  `syncDepFillButton` + `scheduleAutoFill()`（`parseDesign` 末尾）；设置面板 §9.5 可增删搜索根；缺口时「＋」右侧显示
  「自动补齐」按钮（**点一次即消失**）。共 7 个文件改动 + 新建 `tools/fs-api-probe.mjs`。
- **真机 Bug（P58）**：`HttpListenerRequest.QueryString` 用 **GBK** 解百分号转义 ⇒ 中文工程路径全读不到
  （`op=list` 只回 `.e2e-tmp`、`op=read/index` 404）。修法 = 自写 `QueryParam()`（`RawUrl` + `Uri.UnescapeDataString`）。
- **探针自身缺陷 2 条**：① 虚拟文件系统缺 `proj/rtl/core/top_mod.sv`（模块定义找不到）；② `ensure(i < parts.length ? cur : parent)`
  把每个目录写成**自环** ⇒ 递归索引走不进子目录。均已修。
- **验证**：`build.ps1` csc exit 0（exe 22,259,200 B）；`dep-probe`（§1~§8）全 PASS；`regression 79/79`；
  `source-panel-probe` / `dock-probe` / `split-drag-probe` 全过；`e2e-ui 88/88`；`e2e-rtl 66/66`；`e2e-sim 0 失败`；
  `ui-audit 0 违规`；`fs-api-probe`（真 exe，端口 17817）全 PASS；`exe-smoke` 全局对象齐全、无异常。
- **UI 诉求（未做，待后续）**：把「自动补齐」按钮文案/位置定稿（已按用户口径：“＋”右侧、「点一次就消失」）。
