# 2026-09-09 WavePaint 工作日志

## 本轮目标

1. 整理项目全部记忆文档，包括 `.workbuddy/`、`docs/` 与旧总纲。
2. 将所有跨 AI 记忆统一收敛到 `memory/`。
3. 按“项目知识 / 工作流 / 需求 / 进展 / 日志 / 坑 / 决策 / 路线图 / 交接”分类。
4. 删除其他记忆目录，只保留 `memory/` 一个记忆入口。

## 做了什么

- 新建并完善 `memory/01-PROJECT.md`
  - 项目定位、关键事实、演进史、总体架构、模块职责、数据流、数据模型、唯一 API、构建命令。
- 新建并完善 `memory/02-WORKFLOW.md`
  - 标准任务流程、C1~C18 硬性约束、验证门槛、构建 SOP、交付 SOP、记忆维护义务、跨 AI 工作流。
- 新建 `memory/03-REQUIREMENTS.md`
  - 汇总 #1~#80 全部需求，按功能、Bug、仿真、解混淆、批次、参数化、记忆体系分组。
- 新建 `memory/04-PROGRESS.md`
  - 当前状态快照、里程碑时间线、已完成主线、下一步任务、未排期方向、近期关键结论。
- 新建 `memory/05-LOGS.md`
  - 每日日志索引、阅读顺序、日志模板、维护规则。
- 新建 `memory/06-PITFALLS.md`
  - P01~P18 历史坑库，覆盖仿真、stride、值解析、菜单、构建、环境、记忆同步等。
- 新建 `memory/07-DECISIONS.md`
  - D01~D10 关键技术决策，避免后续 AI 重新争论。
- 新建 `memory/08-ROADMAP.md`
  - Verdi 借鉴 P0~P3、开源借鉴清单、远期方向、不做/暂缓项。
- 新建 `memory/09-HANDOFF.md`
  - 下一任 AI 交接页，包含当前状态、必读顺序、下一步任务、命令、环境注意点、交付提醒。
- 重写 `memory/INDEX.md`
  - 作为唯一入口，定义读我顺序、文件结构、维护铁律、禁止事项。

## 关键结论

- `memory/` 现在是项目唯一记忆目录。
- 下一任 AI 只需从 `INDEX.md` 进入，即可按顺序获得完整上下文。
- `.workbuddy/`、`docs/attic/` 等旧记忆源的内容已全部收敛到 `memory/`。
- 经用户确认，`.workbuddy/` 与 `docs/` 已物理删除。

## 验证结果

- 记忆文件结构完整：`01~09` + `INDEX` + `logs/`。
- `INDEX.md` 与实际文件一一对应。
- 未改动任何运行时代码，因此无需重建 exe。

## 遗留问题

- 无功能阻塞。
- 无遗留待办；项目已实现“只留 `memory/` 一个记忆目录”。
- 下一步主线：#75 Verdi 借鉴 P0。

## 交付提醒

- 本轮只整理记忆，不涉及 exe 交付。
- 下一任 AI 从 `memory/INDEX.md` 开始。

---

## 第二轮：#75 Verdi 借鉴 P0 完成（2026-09-09）

> 接上轮：记忆体系就绪后，按 `08-ROADMAP.md` 规划实现 #75 P0。

## 本轮目标

1. 内嵌 CodeMirror 6 代码视图（Verilog 语法高亮，可读可跳转）。
2. RTL Tree 面板（模块/端口/参数/实例，点击跳代码行）。
3. VCD 全路径索引（作用域层次树，为 P1 点变量加波形打地基）。

## 做了什么

- `lib/codemirror.bundle.js`（305KB）：`tools/cm6-entry.js` 经 esbuild 预打包成 IIFE，挂 `globalThis.WPCm`，导出 `createVerilogEditor`；`package.json` 增 devDeps（codemirror / @codemirror/legacy-modes / esbuild），`allowScripts` 放行 esbuild 安装脚本。
- `js/sim/rtl-nav.js`（纯函数）：复用 `engine.parseVerilogDesign()`，为 module/port/parameter/instance 定位「文件 + 行号」；`blankComments` 把注释抹成等长空格（保留换行），保证行号按原文、注释中的 module 字样不干扰。
- `js/sim/vcd-index.js`（纯函数）：`buildVcdHierarchy(parsed)` 沿点分 scope 建链，输出 `{signalCount, scopeCount, tree}`，纯数据树可 JSON 序列化，根作用域信号挂 root。
- `js/sim/rtl-panel.js`（DOM 渲染层）：`installCodeEditor`（CM 挂载 + textarea 数据镜像 + 缺失降级）、`renderRtlTree`（点击 → onJump）、`renderVcdTree`（点击信号 → onSignalPick(完整路径)）；不 import ui-bridge 防循环依赖。
- `index.html` + `ui-bridge.js`：仿真面板新增代码视图 / RTL 树 / VCD 树卡片；`initSourceCodeView` / `setEditorText` / `jumpToEditorLine` / `refreshStructureTrees` / `refreshVcdTree`；`state.vcd` 保存最近一次成功仿真的 `parseVcd` 产物；编辑防抖 350ms 重建 RTL 树；切文件/程序性写销毁重建 EditorView（防 Ctrl+Z 跨文件回滚）。
- `tools/e2e-rtl.mjs`：新增 #75 浏览器级冒烟（真实 Edge + dev-server，9 项 A0~C3）。
- `tools/regression.mjs`：新增 rtl-nav（5 项）+ vcd-index（4 项）纯函数用例，49 → 58。

## 关键结论

- CM 不可用时 textarea 自动 fallback，`installCodeEditor` 返回 `active=false` 并隐藏 CM host —— 任何旧路径零影响。
- 用户编辑 onChange 同步回 textarea（`syncEditor()` 旧读路径兼容）；`setEditorText` 永不双写。
- VCD 信号完整路径 = `node.path + "." + signal.name`，已按真实 `parseVcd` 产物验证。
- ANSI 头部多行端口行号逐行精确；共享方向关键字单行列表（`input a, b`）同列定位；非 ANSI 定位到体内方向声明行。

## 验证结果

- 语法：`node --check` rtl-nav / vcd-index / rtl-panel / ui-bridge / cm6-entry / e2e-rtl / regression 全过。
- `node tools/regression.mjs`：58/58 全绿（49 基线 + 9 新用例）。
- `node tools/e2e-rtl.mjs`：9/9 通过，零控制台异常（A: CM 挂载 + textarea 镜像；B: RTL 树点击跳转状态栏；C: 真 iverilog 仿真后 VCD 树渲染完整点分路径）。
- `node tools/e2e-sim.mjs`：失败 0 项（ui-bridge 改动后全链路回归）。
- `node tools/probe-param.mjs`：嵌套参数 / $clog2 / sized 字面量 全过。
- exe 重建（C1）：csc exit 0、res 50 项、22,725,120 字节（+334KB）；`rg -a -c "verilog-cm-host"`=5、`rg -a -c "buildVcdHierarchy"`=4 特征串进 exe。
- exe 冒烟：启动真 exe → 端口文件就绪 → core/wpf/doc 对象齐全、画布 true、中文弹窗标题「添加时钟信号」、控制台异常 无。

## 遗留问题

- #75 P1（点变量→加波形、树↔代码双向跳转、信号组入 `.wp`）尚未开始。
- CM 版本 6 目前只读为主（有编辑能力但非 P0 重点）；undo 在切文件后按设计重置。
- `tools/cm6-entry.js` 是构建源、不进 exe；若改 CM 相关源码需重跑 `npx esbuild` 再重建 exe。

## 交付提醒

- 重启应用。
- 从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动。
- 面板版本号自查（`v0.4.0 build 2026-09-09 …`），确认不是旧副本。

---

## 第三轮：#81 Bug 自研修复收尾（2026-09-09）

> 用户报告两类已知 bug：① 仿真服务“有可能不在线”；② 添加信号按钮图标“有时候会消失”。
> 另在本轮调查中发现并修复第三类问题：汉化版教程（help 引导）每次启动隐形重跑，
> 会在 document 上拦截 Enter/Space/方向键、并把高亮标记挂到真实控件上。
> 用户要求“自行研究 + 自测 + 修改”，本文件记录全部定位与验证过程。

## 本轮目标

1. 复现并根治「仿真服务不在线」（瞬时自愈窗口被误报成“服务已死”）。
2. 复现并根治「添加信号按钮图标偶发消失」。
3. 排查教程（tutorial）在随机端口会话下的隐形副作用（键盘拦截 / 控件被 CSS 隐藏）。
4. 用自研 UI 探针 + 真 exe 冒烟验证，重建 exe。

## 做了什么

### Bug1：仿真服务瞬时离线（`js/sim/ui-bridge.js`）

- **根因**：launcher 的 AcceptLoop 异常自愈（RestartServer 同端口重绑，见 P06/#55）会制造
  “服务瞬时不可达”窗口：该窗口内的 `/api/sim` 请求被丢弃，UI 旧实现一律提示
  “请重启应用”——用户以为服务“永久离线”，实际上服务几百 ms 后自己恢复了。
- **修复**：
  - 新增 `probeServerAlive(timeoutMs=800)`：快速 GET `api/ping`（exe 内置服务与
    dev-server 都实现了该路由，WavePaintLauncher.cs:252）区分「进程死」与「自愈窗口」。
  - `/api/sim` 请求挂 45s `AbortController`（`SIM_REQUEST_TIMEOUT_MS`），防 iverilog
    卡死时无限“正在运行...”。
  - 失败分类：fetch/abort/network → 探活；探活成功且未自动重试过 → 静默自动重试一次
    （`simAutoRetried`，用户每次手动点击复位）；探活成功但已重试过 → 提示
    “本地服务在线，但本次请求未完成”（iverilog 超时/请求被丢弃）；
    探活失败 → 保留“请重启应用”指引。
- **验证**：`.e2e-tmp/probe-simfail.mjs` 三场景（S-A selfheal=自动重试 2 次 / S-B dead=
    提示重启 / S-C abort=提示超时）全部 PASS。

### Bug2：添加信号按钮图标消失（`index.html`）

- **根因**（两级）：
  1. 汉化版 CSS 把 `.wp-tutorial-highlight` 与瞬态教程元素一起隐藏
     （`display:none !important`），而教程 JS 恰恰把该 class 标到**真实控件**
     （`#add-signal-btn` / `.tool-btn` / `.dropdown` / 菜单项）上 → 控件被永久隐藏；
  2. 教程在每次启动都隐形重跑（见 Bug3），于是“偶发” = 教程刚好跑到该控件那一刻。
- **修复**：
  - 隐藏列表移除 `.wp-tutorial-highlight`（只隐藏瞬态遮罩/气泡/箭头/进度条）。
  - 新增防御规则：`.tool-btn/.dropdown/.menu-item/.submenu li/input.wp-tutorial-highlight`
    保持 `visibility:visible; pointer-events:auto; animation:none; box-shadow:none`，
    `.tool-btn`/`.dropdown` 再补 display 修正 —— 即使将来教程重跑也不会再藏控件。
- **验证**：`.e2e-tmp/probe-addbtn.mjs`（基线/明暗主题/菜单 5 次循环加信号/窄窗口 440~
    1440/缩放后）ALL PASS；`.e2e-tmp/probe-css.mjs`、`.e2e-tmp/probe-tree.mjs` 辅助定位。

### Bug3：教程隐形重启 + 键盘拦截（`index.html` + `js/editor/file-menu.js`）

- **根因**：exe 每次启动用随机端口 ⇒ Edge 的 localStorage 绑定端口 ⇒ 每会话都是全新
  localStorage ⇒ `wavepaint_tutorial_done` 永不保留 ⇒ 教程每次启动都隐性重跑；它
  （a）在 document 上拦截 Enter/Space/方向键（键盘“失灵”观感），（b）往真实控件挂
  高亮 class（Bug2 触发器），而汉化版把教程 UI 整体隐藏了 → 用户只见副作用不见引导。
- **修复**：
  - `index.html`：在 `<head>` 内联脚本、**核心 `clean.js` 解析前**预置
    `localStorage.wavepaint_tutorial_done='true'`（try/catch 包裹，隐私模式不炸）。
  - `js/editor/file-menu.js`：捕获阶段拦截 `#menu-bar li[data-action="tutorial"]`
    点击 → `collapseMenus()` + `wpAlert` 中文提示“引导教程已在本版本隐藏…”，
    杜绝核心 `removeItem('wavepaint_tutorial_done')+queueTutorial` 重启隐形教程。
- **验证**：`.e2e-tmp/probe-tutorial.mjs` T1（空 localStorage 启动：按钮可见、无高亮、
    无键盘拦截）+ T2（点帮助→教程：弹“教程”中文提示模态、关闭后仍无拦截、done 仍为
    true）ALL PASS。

### 重要纠错（上一轮误诊澄清）

- 曾怀疑“T2 后键盘失灵”是教程键盘钩子未拆；调试后确认：**弹窗打开期间 Enter 被
  模态自身捕获是设计行为**（核心模态 Enter=Escape 时 `preventDefault` + 关闭自己），
  不是教程副作用。判断教程钩子是否残留必须**先关掉模态再探测按键**。
- `wpModalState` 是核心词法全局，`window.wpModalState === undefined`；调试要直接用裸
  `wpModalState`。模态隐藏开关是 overlay 的 `.hidden` class。

## 验证结果

- `.e2e-tmp/probe-tutorial.mjs`：T1/T2 全过（6 项断言，零控制台异常）。
- `.e2e-tmp/probe-addbtn.mjs`：全过（菜单/主题/窄窗口/缩放 全覆盖）。
- `.e2e-tmp/probe-simfail.mjs`：selfheal / dead / abort 三场景全过。
- `node tools/regression.mjs`：58/58 全绿（纯函数/数据模型未被动）。
- `node tools/probe-param.mjs`：嵌套参数 / $clog2 / sized 字面量 全过。
- `node tools/e2e-sim.mjs`：失败 0 项（真实 iverilog 全链路回归）。
- exe 重建（C1）：csc exit 0、res 50 项、22,731,776 字节；
  特征串核验 `simAutoRetried` / `wavepaint_tutorial_done` / `wp-tutorial-highlight` /
  `probeServerAlive` / `SIM_REQUEST_TIMEOUT_MS` 全部在 exe 内。
- 真 exe 冒烟（`tools/exe-smoke.mjs`）：core/wpf/doc 对象齐全、画布 true、中文弹窗
  标题「添加时钟信号」、控制台异常 无。
- `tools/exe-smoke.mjs` 顺手修复：选端口文件改按**最近写入时间**而非“最大文件名”，
  避免残留的旧 `WavePaintClean_port_*.txt` 把冒烟连到已死端口（本次实证踩坑）。

## 遗留问题

- 无功能阻塞。下一主线仍为 Verdi P1（台账 #76），本会话未动 P1。
- 未动 `js/sim/engine.js` 的 stride / 端口匹配 / VCD 回填（C 区最易碎，本轮零触碰）。
- UI 探针目前放 `.e2e-tmp/`（已被 .gitignore 覆盖，不提交）；若后续要长期保留，
  可把 probe-tutorial/addbtn/simfail 提炼成正式 `tools/e2e-*.mjs`（未做，属可选）。

## 交付提醒

- 重启应用、从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动。
- 面板版本号自查，确认不是旧副本。
