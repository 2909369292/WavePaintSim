# 2026-09-09 WavePaint 工作日志

## 本轮目标

1. 整理项目全部记忆文档，包括 `.workbuddy/`、`docs/` 与旧总纲。
2. 将所有跨 AI 记忆统一收敛到 `memory/`。
3. 按“项目知识 / 工作流 / 需求 / 进展 / 日志 / 坑 / 决策 / 路线图 / 交接”分类。
4. 删除其他记忆目录，只保留 `memory/` 一个记忆入口。

## 做了什么

- 新建并完善 `memory/01-PROJECT.md`
  - 项目定位、关键事实、演进史、总体架构、模块职责、数据流、数据模型、唯一 API、构建命令。
- 新建并完善 `memory/02-WORKFLOW.md`
  - 标准任务流程、C1~C18 硬性约束、验证门槛、构建 SOP、交付 SOP、记忆维护义务、跨 AI 工作流。
- 新建 `memory/03-REQUIREMENTS.md`
  - 汇总 #1~#80 全部需求，按功能、Bug、仿真、解混淆、批次、参数化、记忆体系分组。
- 新建 `memory/04-PROGRESS.md`
  - 当前状态快照、里程碑时间线、已完成主线、下一步任务、未排期方向、近期关键结论。
- 新建 `memory/05-LOGS.md`
  - 每日日志索引、阅读顺序、日志模板、维护规则。
- 新建 `memory/06-PITFALLS.md`
  - P01~P18 历史坑库，覆盖仿真、stride、值解析、菜单、构建、环境、记忆同步等。
- 新建 `memory/07-DECISIONS.md`
  - D01~D10 关键技术决策，避免后续 AI 重新争论。
- 新建 `memory/08-ROADMAP.md`
  - Verdi 借鉴 P0~P3、开源借鉴清单、远期方向、不做/暂缓项。
- 新建 `memory/09-HANDOFF.md`
  - 下一任 AI 交接页，包含当前状态、必读顺序、下一步任务、命令、环境注意点、交付提醒。
- 重写 `memory/INDEX.md`
  - 作为唯一入口，定义读我顺序、文件结构、维护铁律、禁止事项。

## 关键结论

- `memory/` 现在是项目唯一记忆目录。
- 下一任 AI 只需从 `INDEX.md` 进入，即可按顺序获得完整上下文。
- `.workbuddy/`、`docs/attic/` 等旧记忆源的内容已全部收敛到 `memory/`。
- 经用户确认，`.workbuddy/` 与 `docs/` 已物理删除。

## 验证结果

- 记忆文件结构完整：`01~09` + `INDEX` + `logs/`。
- `INDEX.md` 与实际文件一一对应。
- 未改动任何运行时代码，因此无需重建 exe。

## 遗留问题

- 无功能阻塞。
- 无遗留待办；项目已实现“只留 `memory/` 一个记忆目录”。
- 下一步主线：#75 Verdi 借鉴 P0。

## 交付提醒

- 本轮只整理记忆，不涉及 exe 交付。
- 下一任 AI 从 `memory/INDEX.md` 开始。

---

## 第二轮：#75 Verdi 借鉴 P0 完成（2026-09-09）

> 接上轮：记忆体系就绪后，按 `08-ROADMAP.md` 规划实现 #75 P0。

## 本轮目标

1. 内嵌 CodeMirror 6 代码视图（Verilog 语法高亮，可读可跳转）。
2. RTL Tree 面板（模块/端口/参数/实例，点击跳代码行）。
3. VCD 全路径索引（作用域层次树，为 P1 点变量加波形打地基）。

## 做了什么

- `lib/codemirror.bundle.js`（305KB）：`tools/cm6-entry.js` 经 esbuild 预打包成 IIFE，挂 `globalThis.WPCm`，导出 `createVerilogEditor`；`package.json` 增 devDeps（codemirror / @codemirror/legacy-modes / esbuild），`allowScripts` 放行 esbuild 安装脚本。
- `js/sim/rtl-nav.js`（纯函数）：复用 `engine.parseVerilogDesign()`，为 module/port/parameter/instance 定位「文件 + 行号」；`blankComments` 把注释抹成等长空格（保留换行），保证行号按原文、注释中的 module 字样不干扰。
- `js/sim/vcd-index.js`（纯函数）：`buildVcdHierarchy(parsed)` 沿点分 scope 建链，输出 `{signalCount, scopeCount, tree}`，纯数据树可 JSON 序列化，根作用域信号挂 root。
- `js/sim/rtl-panel.js`（DOM 渲染层）：`installCodeEditor`（CM 挂载 + textarea 数据镜像 + 缺失降级）、`renderRtlTree`（点击 → onJump）、`renderVcdTree`（点击信号 → onSignalPick(完整路径)）；不 import ui-bridge 防循环依赖。
- `index.html` + `ui-bridge.js`：仿真面板新增代码视图 / RTL 树 / VCD 树卡片；`initSourceCodeView` / `setEditorText` / `jumpToEditorLine` / `refreshStructureTrees` / `refreshVcdTree`；`state.vcd` 保存最近一次成功仿真的 `parseVcd` 产物；编辑防抖 350ms 重建 RTL 树；切文件/程序性写销毁重建 EditorView（防 Ctrl+Z 跨文件回滚）。
- `tools/e2e-rtl.mjs`：新增 #75 浏览器级冒烟（真实 Edge + dev-server，9 项 A0~C3）。
- `tools/regression.mjs`：新增 rtl-nav（5 项）+ vcd-index（4 项）纯函数用例，49 → 58。

## 关键结论

- CM 不可用时 textarea 自动 fallback，`installCodeEditor` 返回 `active=false` 并隐藏 CM host —— 任何旧路径零影响。
- 用户编辑 onChange 同步回 textarea（`syncEditor()` 旧读路径兼容）；`setEditorText` 永不双写。
- VCD 信号完整路径 = `node.path + "." + signal.name`，已按真实 `parseVcd` 产物验证。
- ANSI 头部多行端口行号逐行精确；共享方向关键字单行列表（`input a, b`）同列定位；非 ANSI 定位到体内方向声明行。

## 验证结果

- 语法：`node --check` rtl-nav / vcd-index / rtl-panel / ui-bridge / cm6-entry / e2e-rtl / regression 全过。
- `node tools/regression.mjs`：58/58 全绿（49 基线 + 9 新用例）。
- `node tools/e2e-rtl.mjs`：9/9 通过，零控制台异常（A: CM 挂载 + textarea 镜像；B: RTL 树点击跳转状态栏；C: 真 iverilog 仿真后 VCD 树渲染完整点分路径）。
- `node tools/e2e-sim.mjs`：失败 0 项（ui-bridge 改动后全链路回归）。
- `node tools/probe-param.mjs`：嵌套参数 / $clog2 / sized 字面量 全过。
- exe 重建（C1）：csc exit 0、res 50 项、22,725,120 字节（+334KB）；`rg -a -c "verilog-cm-host"`=5、`rg -a -c "buildVcdHierarchy"`=4 特征串进 exe。
- exe 冒烟：启动真 exe → 端口文件就绪 → core/wpf/doc 对象齐全、画布 true、中文弹窗标题「添加时钟信号」、控制台异常 无。

## 遗留问题

- #75 P1（点变量→加波形、树↔代码双向跳转、信号组入 `.wp`）尚未开始。
- CM 版本 6 目前只读为主（有编辑能力但非 P0 重点）；undo 在切文件后按设计重置。
- `tools/cm6-entry.js` 是构建源、不进 exe；若改 CM 相关源码需重跑 `npx esbuild` 再重建 exe。

## 交付提醒

- 重启应用。
- 从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动。
- 面板版本号自查（`v0.4.0 build 2026-09-09 …`），确认不是旧副本。

---

## 第三轮：#81 Bug 自研修复收尾（2026-09-09）

> 用户报告两类已知 bug：① 仿真服务“有可能不在线”；② 添加信号按钮图标“有时候会消失”。
> 另在本轮调查中发现并修复第三类问题：汉化版教程（help 引导）每次启动隐形重跑，
> 会在 document 上拦截 Enter/Space/方向键、并把高亮标记挂到真实控件上。
> 用户要求“自行研究 + 自测 + 修改”，本文件记录全部定位与验证过程。

## 本轮目标

1. 复现并根治「仿真服务不在线」（瞬时自愈窗口被误报成“服务已死”）。
2. 复现并根治「添加信号按钮图标偶发消失」。
3. 排查教程（tutorial）在随机端口会话下的隐形副作用（键盘拦截 / 控件被 CSS 隐藏）。
4. 用自研 UI 探针 + 真 exe 冒烟验证，重建 exe。

## 做了什么

### Bug1：仿真服务瞬时离线（`js/sim/ui-bridge.js`）

- **根因**：launcher 的 AcceptLoop 异常自愈（RestartServer 同端口重绑，见 P06/#55）会制造
  “服务瞬时不可达”窗口：该窗口内的 `/api/sim` 请求被丢弃，UI 旧实现一律提示
  “请重启应用”——用户以为服务“永久离线”，实际上服务几百 ms 后自己恢复了。
- **修复**：
  - 新增 `probeServerAlive(timeoutMs=800)`：快速 GET `api/ping`（exe 内置服务与
    dev-server 都实现了该路由，WavePaintLauncher.cs:252）区分「进程死」与「自愈窗口」。
  - `/api/sim` 请求挂 45s `AbortController`（`SIM_REQUEST_TIMEOUT_MS`），防 iverilog
    卡死时无限“正在运行...”。
  - 失败分类：fetch/abort/network → 探活；探活成功且未自动重试过 → 静默自动重试一次
    （`simAutoRetried`，用户每次手动点击复位）；探活成功但已重试过 → 提示
    “本地服务在线，但本次请求未完成”（iverilog 超时/请求被丢弃）；
    探活失败 → 保留“请重启应用”指引。
- **验证**：`.e2e-tmp/probe-simfail.mjs` 三场景（S-A selfheal=自动重试 2 次 / S-B dead=
    提示重启 / S-C abort=提示超时）全部 PASS。

### Bug2：添加信号按钮图标消失（`index.html`）

- **根因**（两级）：
  1. 汉化版 CSS 把 `.wp-tutorial-highlight` 与瞬态教程元素一起隐藏
     （`display:none !important`），而教程 JS 恰恰把该 class 标到**真实控件**
     （`#add-signal-btn` / `.tool-btn` / `.dropdown` / 菜单项）上 → 控件被永久隐藏；
  2. 教程在每次启动都隐形重跑（见 Bug3），于是“偶发” = 教程刚好跑到该控件那一刻。
- **修复**：
  - 隐藏列表移除 `.wp-tutorial-highlight`（只隐藏瞬态遮罩/气泡/箭头/进度条）。
  - 新增防御规则：`.tool-btn/.dropdown/.menu-item/.submenu li/input.wp-tutorial-highlight`
    保持 `visibility:visible; pointer-events:auto; animation:none; box-shadow:none`，
    `.tool-btn`/`.dropdown` 再补 display 修正 —— 即使将来教程重跑也不会再藏控件。
- **验证**：`.e2e-tmp/probe-addbtn.mjs`（基线/明暗主题/菜单 5 次循环加信号/窄窗口 440~
    1440/缩放后）ALL PASS；`.e2e-tmp/probe-css.mjs`、`.e2e-tmp/probe-tree.mjs` 辅助定位。

### Bug3：教程隐形重启 + 键盘拦截（`index.html` + `js/editor/file-menu.js`）

- **根因**：exe 每次启动用随机端口 ⇒ Edge 的 localStorage 绑定端口 ⇒ 每会话都是全新
  localStorage ⇒ `wavepaint_tutorial_done` 永不保留 ⇒ 教程每次启动都隐性重跑；它
  （a）在 document 上拦截 Enter/Space/方向键（键盘“失灵”观感），（b）往真实控件挂
  高亮 class（Bug2 触发器），而汉化版把教程 UI 整体隐藏了 → 用户只见副作用不见引导。
- **修复**：
  - `index.html`：在 `<head>` 内联脚本、**核心 `clean.js` 解析前**预置
    `localStorage.wavepaint_tutorial_done='true'`（try/catch 包裹，隐私模式不炸）。
  - `js/editor/file-menu.js`：捕获阶段拦截 `#menu-bar li[data-action="tutorial"]`
    点击 → `collapseMenus()` + `wpAlert` 中文提示“引导教程已在本版本隐藏…”，
    杜绝核心 `removeItem('wavepaint_tutorial_done')+queueTutorial` 重启隐形教程。
- **验证**：`.e2e-tmp/probe-tutorial.mjs` T1（空 localStorage 启动：按钮可见、无高亮、
    无键盘拦截）+ T2（点帮助→教程：弹“教程”中文提示模态、关闭后仍无拦截、done 仍为
    true）ALL PASS。

### 重要纠错（上一轮误诊澄清）

- 曾怀疑“T2 后键盘失灵”是教程键盘钩子未拆；调试后确认：**弹窗打开期间 Enter 被
  模态自身捕获是设计行为**（核心模态 Enter=Escape 时 `preventDefault` + 关闭自己），
  不是教程副作用。判断教程钩子是否残留必须**先关掉模态再探测按键**。
- `wpModalState` 是核心词法全局，`window.wpModalState === undefined`；调试要直接用裸
  `wpModalState`。模态隐藏开关是 overlay 的 `.hidden` class。

## 验证结果

- `.e2e-tmp/probe-tutorial.mjs`：T1/T2 全过（6 项断言，零控制台异常）。
- `.e2e-tmp/probe-addbtn.mjs`：全过（菜单/主题/窄窗口/缩放 全覆盖）。
- `.e2e-tmp/probe-simfail.mjs`：selfheal / dead / abort 三场景全过。
- `node tools/regression.mjs`：58/58 全绿（纯函数/数据模型未被动）。
- `node tools/probe-param.mjs`：嵌套参数 / $clog2 / sized 字面量 全过。
- `node tools/e2e-sim.mjs`：失败 0 项（真实 iverilog 全链路回归）。
- exe 重建（C1）：csc exit 0、res 50 项、22,731,776 字节；
  特征串核验 `simAutoRetried` / `wavepaint_tutorial_done` / `wp-tutorial-highlight` /
  `probeServerAlive` / `SIM_REQUEST_TIMEOUT_MS` 全部在 exe 内。
- 真 exe 冒烟（`tools/exe-smoke.mjs`）：core/wpf/doc 对象齐全、画布 true、中文弹窗
  标题「添加时钟信号」、控制台异常 无。
- `tools/exe-smoke.mjs` 顺手修复：选端口文件改按**最近写入时间**而非“最大文件名”，
  避免残留的旧 `WavePaintClean_port_*.txt` 把冒烟连到已死端口（本次实证踩坑）。

## 遗留问题

- 无功能阻塞。下一主线仍为 Verdi P1（台账 #76），本会话未动 P1。
- 未动 `js/sim/engine.js` 的 stride / 端口匹配 / VCD 回填（C 区最易碎，本轮零触碰）。
- UI 探针目前放 `.e2e-tmp/`（已被 .gitignore 覆盖，不提交）；若后续要长期保留，
  可把 probe-tutorial/addbtn/simfail 提炼成正式 `tools/e2e-*.mjs`（未做，属可选）。

## 交付提醒

- 重启应用、从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动。
- 面板版本号自查，确认不是旧副本。

---

## 第四轮：#82 遗留 Edge 窗口停在死随机端口 → “直接无法仿真”根治（2026-09-09）

> 用户报障：**存在仿真失败的问题，直接无法仿真**。
> 表面是“仿真功能坏了”，实际是用户正看着的 Edge 页面停在上一个已死实例的随机端口上；
> 该页面同源 `/api/*` 全部断连，所以点「运行仿真」必失败。本轮在 launcher 层根治
> 「死窗口越积越多 + 旧实例永不退出 + 新实例只开窗不收窗」的整条根因链。

## 本轮目标

1. 实证定位“直接无法仿真”的真根因（先排除服务与代码，再看桌面窗口状态）。
2. 修复 launcher：开新窗前自动收拢全部遗留 WavePaint 窗口（只保留新开的那一个）。
3. 真机验证：死窗口 5 → 1、新实例可完整仿真、回归全绿、exe 重建并核验特征串。

## 做了什么

### 排查结论（先实证再动手）

- 当前存活服务（PID 47120，端口 6856，build 2055db3）本身健康：headless Edge 直连
  实测点「运行仿真」成功出 4 个输出信号，无 console 异常、无网络失败 → **不是服务死**。
- 真凶：用户 Edge 窗口地址栏停在 `127.0.0.1:9245` —— 当天 02:59 启动后已退出的旧实例
  （PID 44520）的随机端口；服务死后该页面所有 `/api/*` 断连 → 用户以为“仿真工具坏了”。
- 桌面当时存在 5 个同名「WavePaint 波形编辑」顶层窗口（hwnd 全部挂在 msedge 40436 下）：
  部分指向 6856（活实例）、部分指向 9245/6150（已死实例）。Edge 把同 profile 的
  `--app` 窗口“还原”成一个浏览器进程里的多个 HWND，**无法按进程命令行区分 URL**。
- 根因链：
  1. 重建/崩溃/手动杀 exe 时，其 Edge 窗口（指向当时的随机端口）不会自己消失；
  2. launcher 的 `HasWindow()` 只看 msedge 进程 `MainWindowTitle`，残留任意同名窗口
     就判定“有窗口在服务” → 旧实例永不满足退出条件（需无窗口>12s 且 idle>20s）；
  3. 旧实例不退出 → 旧端口文件残留 + 旧页面继续在桌面 → 死端口窗口越积越多；
  4. `OpenExistingInstance()` 只会新开一个窗口、从不收旧窗口 → 每次重启/重建都累积；
  5. 用户点开其中一个死窗口 → 同源 `/api/*` 全断 → “直接无法仿真”假象。

### 修复（`WavePaintLauncher.cs`，单文件 +80 行）

- 新增 `CloseLegacyWindows()`：
  - `EnumWindows` 枚举全部**可见**顶层窗口，标题含 `WavePaint` / `WaveWorkbench`
    （忽略大小写）者入列；
  - 逐个 `PostMessage(WM_CLOSE)`；随后轮询最多 ~2.5s（500ms×5）等窗口消失，不等也不阻塞；
  - 写一行日志 `CLOSE-LEGACY n window(s)`（`%TEMP%\WavePaintClean_sim.log`）。
- 调用点 1：`MainCore()`（全新启动、单实例互斥体已拿到）`Process.Start(psi)` **之前**；
- 调用点 2：`OpenExistingInstance()`（已有实例在跑、本进程将退出）`Process.Start(psi)`
  **之前**。
- 语义：同一时刻桌面只保留一份指向“最新一次启动”的 WavePaint 页面；死端口页面、
  重复页面、旧构建页面一律先收再开。
- 收尾审查确认：空闲退出阈值（无窗口>24 tick=12s 且 idle>20s）远大于收窗窗口期
  （~2.5s），不会因收旧窗触误退出；BOM（EF BB BF）保留。

### 取舍（详见 `07-DECISIONS.md` D13）

- 接受“再次双击 exe（已有活窗口）会关旧开新”的副作用：若活页上有未保存内容会被关掉。
  该路径旧行为是“再开一个重复窗口”，虽不丢数据但正是窗口累积的源头。
- 否决“只关死端口窗口”：无法从窗口标题/进程命令行区分窗口指向哪个端口（Edge 同 profile
  合并后无 `--app=` 命令行），需要 CDP 逐窗探 URL，复杂度高且不稳。

## 验证结果

- 构建（C1）：`taskkill /F /IM WavePaintClean.exe` → `.\build.ps1` → `csc exit: 0`、
  res 50 项、22,733,312 字节；exe 时间戳刷新。
- 特征串（C8）：`rg -a -c` 命中 `CloseLegacyWindows`=1 / `EnumWindows`=1 / `WM_CLOSE`=1；
  `CLOSE-LEGACY` 是 C# 字符串字面量按 UTF-16 存 #US 堆，普通 rg 匹配不到属正常。
- 真机收窗：新实例启动后服务端日志 `2026-09-09 09:10:34Z CLOSE-LEGACY 5 window(s)`，
  桌面 WavePaint 窗口 5 → **1**（其余全部被 WM_CLOSE 收走）。
- 新实例端到端仿真（端口 3521，headless 直连 exe 内嵌页面）：点「运行仿真」→
  「仿真完成：4 个输出信号…」；零 console 异常、零网络失败。
- 真实前台窗口（UIA AutomationId=`sim-run` 真实点击）：服务端日志新增
  `09:11:22Z ENTER api/sim → RUN → RESULT`（全链路通）。
- `node tools/regression.mjs`：58/58 全绿。
- `node tools/e2e-sim.mjs`：失败 0 项。

## 遗留问题

- 若用户仍从旧副本/旧路径启动，或旧 exe 进程仍活着（互斥体被占），新 exe 会走
  `OpenExistingInstance` 收窗并打开旧实例页面 —— 交付时务必提醒：先退出所有
  WavePaintClean 进程 + 关掉旧窗口，再从唯一路径启动一次。
- #76 Verdi P1 未动，仍为下一主线。
- `CloseLegacyWindows` 只过滤“标题”，不校验窗口归属；多用户会话/其他同名应用窗口
  理论上会被一并收走（当前单实例单会话场景下可接受）。

## 交付提醒

- 重启应用；先 `taskkill /F /IM WavePaintClean.exe`（或任务管理器结束）再启动。
- 从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动。
- 面板版本号自查（新 build 时间 09:09 前后），确认不是旧副本。
- 桌面不应再累积多个「WavePaint 波形编辑」窗口；若仍出现多个 = 仍在使用旧 exe。

---

## 第五轮：Verdi 功能规划（①查看波形 ②添加信号 ③top→例化模块源码 + 其它好用功能）（2026-09-09）

> 用户要求：「详细研究 Verdi 中以下功能和其他与本项目相关的好用的功能，将其规划到本项目中：
> ① 查看波形；② 添加信号；③ 打开一个 top 时，自动查找所有例化的模块的源码文件。」
> 本轮性质 = **研究 + 规划**，未动任何运行时代码，无需重建 exe。产出 = 真实 Verdi 交互语义梳理、
> WavePaint 差距对照、台账 03 表 H 拆解登记 #83~#87、04/09 同步、本日志。

## 本轮目标

1. 用公开资料（Verdi/nWave 官方习惯 + 中文社区博客交叉验证）弄清三大功能在 Verdi 里的**真实交互**。
2. 逐项对照 WavePaint 现状（读真实代码，不做假设），找出「语义等价已具备 / 缺失 / 需补齐」三档。
3. 把差距拆成可分批落地的台账条目（新增 #83~#87，不占用/错乱已有 P1=#76、P2=#77 编号）。
4. 同步 03 / 04 / 09 记忆并提交，交付给用户一份对照表 + 落地清单，先问实现范围、不擅自开代码。

## 做了什么

### Verdi 真实交互语义（研究结论，防下次 AI 再查）

- **① 查看波形（nWave）**：三层 pane = 信号列表区 / 波形区 / 消息区。`f`=fit，`z`/`Z`=缩放；
  `h`=切换「信号显示完整层次路径 vs 叶名」；`L`/`Shift+L`=重新加载仿真结果文件（改 RTL 重编后再看）；
  `b`/`e`=游标移到波形起点/终点；A/B 游标对照区显示 A、B、A-B（Δ）时间与值；右键可改 radix。
- **② 添加信号（Get Signals / 源码联动）**：`g` 打开 Get Signals 层次树，勾选/双击信号入波形；
  Ctrl+W=把**源码里当前选中**的信号加进波形；Ctrl+4=把鼠标所在模块/实例的**全部端口**加入波形；
  也可从 nTrace 层次/源码直接拖进波形区；信号组 = 波形区黄线括号 + 右键 New Group/Rename/Add to Waveform。
- **③ top → 例化模块源码**：Verdi 启动时用 `-f filelist.f`（或 `-y/-v` 库目录、相对 include 路径）
  **把所有 RTL 源码文件加载进会话**，elaborate 后 nTrace 显示完整层次；点击/双击任一例化模块即打开
  该模块的**定义源码文件**。关键：Verdi **不会自动全盘扫盘**，“自动找源码”的成立前提 = 文件已在
  filelist/会话内 + 模块定义跳转语义正确。若某模块源码不在 filelist 内则无源码可跳。
- 其它好用功能（与本项目相关的）：Active Annotation（nWave 游标时刻 → 源码行内直接显示各信号值，
  类似 P2=#77 目标）；原理图 nSchema（生成门级图，较重，本项目不做）；X 首现追溯 / 两次波形 diff
  （= 已登记的 P3=#78 方向）。

### WavePaint 现状对照（读代码实证）

- **① 查看波形**：已有 zoom in/out/fit、A/B 游标 + Δ 测量（`measure.js`）、Bus radix 右键菜单与
  bit 值菜单（`#19`）、GroupManager、undo/redo、时钟/复位预填。**缺**：reload 重载视图、完整层次
  路径显示开关、游标快速移首末、多 pane 值样式层级 —— 登记 #84。
- **② 添加信号**：`js/sim/ui-bridge.js` 的 `addPortSignalsToCanvas()`（L767）把 DUT input/inout 经
  `createPortStimulus` 加画布；output 由 `vcdToProjectOutputs` 回填后 `toNativeSignal`（L247）注入画布
  （L300/L699/L797）。VCD 树已能列出全部信号（含完整路径 title），但 `renderVcdTree` 的 `onSignalPick`
  目前只 `setStatus`（ui-bridge.js L424）——**没有“点信号→加入波形”** → 登记 #85（= #76 第一步先行）。
- **③ 实例 → 定义源码**：P0 的 `rtl-nav.js buildRtlNav` 已为每个 module/port/param/instance 定位
  「文件 + 行号」，点击 tree 跳源码（跨文件先切 tab）。但实例节点只含 moduleName/instanceName/line，
  **点实例只能跳到“例化点行”，不能跳到被例化模块的定义行/定义文件** → 登记 #86。另：源文件目前只能
  手动粘贴 + `addFile()` 空命名，**没有磁盘文件导入入口**（Verdi filelist 语义的本地等价缺口）→ 也归 #86。

### 拆解登记（台账 03 表 H，新增 #83~#87）

- **#83** 总登记：本轮规划完成，未动代码。
- **#84** 波形查看增强（①）：reload / 层次路径显示开关 / 游标移首末，只对齐信息层级不引入 nWave 全量。
- **#85** 点信号 → 加波形（② = #76 第一步，**建议先行**）：复用 `state.vcd`+`buildVcdHierarchy`+
  `toNativeSignal`；补未 dump 提示、多实例歧义选择、加入后滚动可见。
- **#86** 实例 → 模块定义源码跳转 + 源码文件导入（③）：按 moduleName 在全量模块表跨文件找定义行；
  新“导入源码文件”入口读 `.sv/.v` 进 `state.files`。
- **#87** 其它：Ctrl+W / Ctrl+4 / 信号组管理（与 #76 信号组入 `.wp` 合流）/ Active Annotation（与 #77 合流）。

## 关键结论

- **Verdi ③ ≠ 全盘自动扫盘**：是“filelist/源码全录入 + 模块定义跳转语义正确”。WavePaint 要等价，只需
  ① 实例节点跨文件跳定义 moduleLine（现数据全齐，纯加跳转），② 给“磁盘导入源码文件”一个入口。
- **#85/#86 所需数据已 100% 就绪**（VCD 全路径、RTL 行号、toNativeSignal 注入链路、跨文件切 tab 逻辑），
  属纯前端接线 + 少量新 UI，不触碰 engine.js 最易碎区（C9 stride/端口匹配/VCD 回填不动）。
- 本轮未动任何运行时代码（js/html/css/lib 零改动），因此 **C1 重建 exe 不触发**；只改 `memory/` 文档。

## 验证结果

- 代码核对：`ui-bridge.js` L247/300/699/767~921（注入链路）、L424~437（onSignalPick/onJump 现状）、
  `rtl-nav.js`（实例节点字段与行号定位）、`vcd-index.js`（完整路径口径）、`rtl-panel.js`（渲染层）
  均与登记描述一致。
- 记忆同步：03 表 H（#83~#87）、04 §1 快照 + §2 时间线 + §4.5、09 §1/§3/§9 已更新。
- 本轮为纯文档变更，无测试/构建需求；工作区仅 `memory/` 三个文档待提交。

## 遗留问题

- 规划尚未拍板实现范围：#84/#85/#86/#87 及原 #76 剩余项，等用户确认后分批发。
- 建议落地顺序 #85 → #86 → #84；若用户点名只做某项（如「先做 ③ 的源码导入」）按用户要求调整。
- 实现任一代码项后：`node tools/regression.mjs` + `node tools/e2e-rtl.mjs`（真实 Edge 需用户环境）+
  C1 重建 exe + 特征串核验 + C2 commit/push + 记忆同步。

## 交付提醒

- 本轮纯规划，无 exe 变更；交付内容 = 对照表 + 落地清单（见会话最终回复）。
- 用户确认实现范围后，下一个 AI 从 03 表 H / 04 §4.5 / 本日志第五轮继续。

---

## 第六轮：#88~#92 存量功能完善（用户点名 5 项，Verdi 远期暂停）

### 第 1 步（Task3 = #90）：步数/子步数 ▲/▼ 微调按钮（2026-09-09）

## 本轮目标

- 用户第六轮只做存量完善 5 项，不做 Verdi 远期。第一项 = 工具栏「步数/子步数」两个 number
  spin 加 ▲/▼ 按钮，点击 ±1（用户口语「复数/子复数」= 步数/子步数）。

## 做了什么

- `index.html`：两个 `.step-input-wrapper` 内各追加 `.step-stepper`（两枚 `.step-arrow`：
  `data-target=sample-spin|substep-spin`、`data-step=1|-1`）。
- `css/wavepaint.e7b903ef.css`：末尾追加 `.step-stepper`（竖排两枚）/`.step-arrow`（18×12px、
  用 CSS 变量、hover/active/focus-visible 态）。
- `js/editor/resize.js`：新增 `wireStepSteppers()`（feature 内 ready 后执行一次）——
  document capture `pointerdown`：
  1. 命中 `.step-arrow` 才处理，否则直接返回（不干扰其它按钮/画布）；
  2. `e.preventDefault()` 防止点击抢走输入框焦点（避免 blur 产生第二次 change）；
  3. `e.stopPropagation()` 阻断其它捕获监听误判；
  4. 按 spin 的 min/max 收敛后 `base+delta`，写回 `input.value`；
  5. 派发 `new Event('change',{bubbles:true})` → 复用 resize.js 既有 document capture
     change → `readSpins()` + `resizeSignals()`（含 `pushUndoSnapshot`，Ctrl+Z 可撤销）。
- 设计取舍：**不直接在按钮里改模型、不开第二套 resize**；全部走既有 change 通道，
  与手输/回车行为 100% 一致。非法中间态（`input.value` 非数字）不做任何事，交给用户
  回车/失焦处理。

## 验证结果

- `node --check js/editor/resize.js` 通过。
- `node tools/regression.mjs`：58/58。
- `tools/e2e-ui.mjs` 新增 D2 段（真实 Edge headless）：
  - 步数 ▲：spin 与模型 8→9；▼×2：9→7（模型同步提交）；
  - 子步 ▲：2→3；▼×2：3→1；
  - 下限收敛：步数=4 时 ▼ 不再降（模型保持不动）；
  - 上限收敛：子步=16 时 ▲ 不再升；
  - 用例结束把 spin+模型恢复到进入前状态（steps=8/subs=2），E 段不受污染。
  - 结果 42/42 通过（原 36 项 + D2 6 项；环境 404/控制台异常均为 0）。
- ⚠ exe 尚未重建：等 #88/#89/#91/#92 完成后统一 C1 重建 + C8 特征串核验
  （特征串 `stepSpinStep` 备用）。

## 遗留问题 / 下一步

- 下一项 Task1（#88）：编辑模式点击 Vector 不再进框选。动码前必须先重读
  `js/editor/value-input.js` 与 `js/editor/selection.js` 核实 onMouseDown 分支。

## 交付提醒

- Task3 已按 C2 单独 commit（`git log` 见 #90），push 到 `main`。
- exe 未重建属预期（本批收尾统一重建），最终交付前必须重建 + 版本号自查。

---

### 第 2 步（Task1 = #88）：编辑模式点击 Vector 不再自动变框选（2026-09-09）

## 本轮目标

- 用户点名第六轮第 1 项：「修复编辑模式 Bug：编辑模式下点击 bus 类型信号会自动变成框选模式」。
- 期望行为：画笔/无激活工具下点击 Vector 应像 Bit 一样**只弹值输入弹窗**；
  真正需要框选时用 Ctrl/⌘+拖动 或 select 工具，且框选会话结束自动切回画笔。

## 做了什么

- `js/editor/value-input.js`（onMouseDown/onMouseUp 重构，+36/−17）：
  - 旧实现把 `ctrlKey||metaKey||isVector` 一锅端切 select 工具并放行核心原生 range
    selection → Vector **单击**也进框选（用户反馈的 bug 根源）。
  - 新三分支：
    - Ctrl/⌘（Bit/Vector）→ `mode='native'`：切 select 交给 selection.js（框选保留）；
    - 非 Ctrl Vector → `mode='vector-paint'`：**不切工具**，`stopImmediatePropagation()`
      + `preventDefault()` 阻断核心 canvas 冒泡的 vectorSelecting（否则核心在 mouseup
      会自弹旧式「Vector Value」框，解析/撤销口径与本模块不一致）；
    - Bit 普通 → `mode=null`：原样放行 draw.js（核心先画一笔再弹窗 —— C 段 e2e
      依赖此既有行为，本次零触碰）。
  - onMouseUp：`native` 照旧放行；`null` 与 `vector-paint` 统一 —— 未拖动（>4px）且
    仍为画笔 → `openValuePrompt`（Vector 弹「矢量值」，走 `wpf.parseValue`，
    输入 A → 10）；已拖动 → 不弹不写。
- `tools/e2e-ui.mjs` 新增 F 段（C 段后、D 段前，视口 1600×1200，建 fbit/fvec）：
  - F1 单击 Vector → 「矢量值」弹窗 + promptActive，tool 仍 paint、无框选工具条/选区；
  - F2 输入 `A` 回车 → 主步 `0..S-1` 格值=10（数字）、标签='A'，其余格不变，弹窗关闭；
  - F3 拖动（p0→pS，>4px）→ 不弹窗、无框选工具条/选区、值标签完全不变
    （验证核心 vectorSelecting 确实被阻断）；
  - F4 Ctrl+单击（CDP `modifiers:2`）→ `wpf.selection` Region=1 主步、工具条出现、
    tool 已切 select；Esc → 清选框/工具条并自动切回 paint（`onSelectionSessionEnd`）；
  - F4.5 会话结束后再单击 Vector 仍弹「矢量值」（不滞留 select）。
- F 段两处测试代码自身 bug 已修：① `__Fcell` 直接返回对象却被 `JSON.parse`（与 E 段
  `__pt` 同坑）→ 改规范化；② `__Fstate` 的 `sel` 字段在无选区时是 `undefined` 被
  JSON.stringify 丢弃 → 归一化为 `|| null`。

## 验证结果

- `node --check js/editor/value-input.js` + `node --check tools/e2e-ui.mjs` 通过。
- `node tools/e2e-ui.mjs`：**50/50 全绿**（42 基线 + F 段 7 项；真实 Edge headless，
  零 404、零控制台异常）。关键 PASS：
  - F1 单击 Vector 弹「矢量值」且不切 select/无工具条/无选区；
  - F2 输入 A → 主步格 10、标签 A、弹窗关闭；
  - F3 拖动不弹窗、不框选、值不变；
  - F4 Ctrl+单击正常框选 + Esc 自动回 paint；F4.5 不滞留 select。
- `node tools/regression.mjs`：58/58 全绿。
- ⚠ exe 尚未重建：等 #92/#91/#89 剩余项完成后统一 C1 重建 + C8 特征串核验
  （本改动特征串候选 `vector-paint` / `openValuePrompt`）。

## 遗留问题 / 下一步

- 下一项 Task5（#92）：框选输入值后点击画布其它处自动提交（不需点确认/回车）。
  涉及 `js/editor/selection.js`（commitInput/dismissBar）与 value-input/`__core`
  capture 顺序，动码前先读 04 §4.6 Task5 线索。

## 交付提醒

- Task1 已按 C2 单独 commit + push 到 `main`（建议消息含 `feat(#88)+docs`）。
- exe 未重建属预期（本批收尾统一重建），最终交付前必须重建 + 版本号自查。

---

### 第 3 步（Task5 = #92）：框选输入后点画布其它处自动提交（2026-09-09）

## 本轮目标

- 用户点名第六轮第 5 项：「在对所有信号进行编辑时，框选一定信号并输入值之后，点击
  其他地方可以自动将值进行输入，而不需要点击确认或回车等」。
- 期望行为：工具条输入框里**真实键入过值**后，点画布/画布外自动提交；空输入点别处 =
  取消（不写值）；Esc 仍为取消；程序/自动化直改 `input.value`（残留半输入）不得被误当
  键入提交（E6 语义）。

## 做了什么

- `js/editor/selection.js`（#92 实现，文件头注释同步补充）：
  - `showBar()`：input 监听 `input` 事件 → `userTyped=true` +
    `setAttribute('data-typed','1')`；挂 `bar.__commitTyped = function(){ if(!userTyped)
    return false; commitInput(); return true; }`。只有 `input` 事件（真实键入/粘贴）算
    可提交 —— 程序直改 `input.value`（E6 残留文本/自动化）不触发，保住 E6「直接赋值
    残留文本点画布 = 取消不写」语义。
  - `onMouseDown()`（画布按下）：原 `hideBarKeepSelection()` 改为
    `if (bar) dismissBar({ commitIfTyping: true })` —— 有键入先 commitInput 收条返回
    true，本次鼠标会话继续（拖动=提交后开新区）；未键入/空值 → 取消收条。
  - 画布外 mousedown capture（非 canvas、非 bar 内）→ `dismissBar({commitIfTyping:true})`；
    Esc / resize 不带参数 → 仍为「取消」。
  - `dismissBar(opts)`：`commitIfTyping` 且 `bar.__commitTyped()` 返回 true（已提交收条）
    → return true，避免二次 hideBar；否则 hideBar。
  - `hideBarKeepSelection()` 已是死代码 → 删除。
- `tools/e2e-ui.mjs` 新增 G 段（视口 1600×1200；建 gbit/gvec，gvec radix=Hex；S=3）：
  - G1 单击 gbit 主步1 → 输入 1 → 点画布其它格（gvec 6S）→ 自动提交该主步、旧选区/工具
    条清除、工具仍 select；
  - G2 拖动 gvec 0..2S → 输入 A → 再拖新区域（gbit 4S..5S）→ 旧区自动提交 10/标签 A +
    新选区在 gbit、bar 重开聚焦（会话延续）；
  - G3 空输入 + 点画布外（inert div）→ 值不变、工具条关、选区清；
  - G4 输入 1 + 点画布外 → 自动提交该区并关闭。
  - 新增 `parkBarAway()`：把浮动工具条挪到离拖拽点最远的角落，避免盖住下一段拖拽起点。

## 验证结果

- `node --check js/editor/selection.js` + `node --check tools/e2e-ui.mjs` 通过。
- `node tools/e2e-ui.mjs`：**60/60 全绿**（真实 Edge headless，零 404、零控制台异常）。
- `node tools/regression.mjs`：58/58 全绿。
- ⚠ exe 尚未重建：等 #91/#89 剩余项完成后统一 C1 重建 + C8 特征串核验。

## 调试过程（G2/G4 首跑失败归因，防后人踩坑）

- 首跑 57/60：G2 两条 + G4 一条失败。临时 `tools/dbg-g92.mjs`（cdp 直连）归因后已删：
  - **G2**：第二段拖拽起点 `pC(283,126)` 落在工具条矩形 `(187,111)-(508,154)` 内 →
    mousedown 命中工具条按钮（触发 suppressCommit），第二段拖动根本没发生，旧区自然没
    提交。这是**测试坐标遮挡**，非产品 bug —— E 段早有 parkBar 先例；G 段补
    `parkBarAway()` 解决。
  - **G4**：G2 失败的级联脏状态（G2 遗留 open bar + typed 'A'，G3 拖动时被先行提交导致
    后续错位）→ G2 修复后 G4 自动转绿。
- 附加环境坑：e2e-ui 依赖固定端口 9531/8949；`dbg-g92.mjs` 结束后其 headless Edge /
  dev-server 未退出、占着 9531 → 再次跑 e2e 时新 Edge 绑不上端口，**连到上次调试会话的
  脏页面**，出现 D/E/F/G 大面积与改动无关的假失败（37/60）。跑 e2e 前先查
  `Get-NetTCPConnection -State Listen | ? LocalPort -in 9531,8949`，必要时按命令行杀
  `.e2e-tmp` profile 的 msedge 与 dev-server node。

## 遗留问题 / 下一步

- 下一项 Task4（#91）：步数/子步变化时 Clock 自动填充防全 0/全 1（疑因
  `dOld===dNew` 重建抹平子步/跨格 0101，见 04 §4.6 #91）。

## 交付提醒

- Task5 已按 C2 单独 commit + push 到 `main`（建议消息含 `feat(#92)+docs`）。
- exe 未重建属预期（本批收尾统一重建），最终交付前必须重建 + 版本号自查。

---

### 第 4 步（Task4 = #91）：步长/子步变化时 Clock 自动填充防全 0/全 1（2026-09-09）

## 本轮目标

- 用户点名第六轮第 4 项：「优化步长改变时的 Clock 填充。目前步长改变时，clock 会
  直接更新成全 1 或全 0（具体不清楚，反正不是一个正常的 clock 信号）。希望在步长
  增加的时候，clock 可以自动填充」。
- 实证根因：旧 `resize.js` 对每个信号一律「每主步抽首格(main) → 主值截断/延拓 →
  把主值铺满新长度每个格子」。自动时钟行实际以**逐格 1,0,1,0…** 存储（ui-bridge
  `readWaveDocument` 的 clockCells 口径：width≤1 行把 rawValues 逐格驱动，每个主步
  内必须内含翻转才有上升沿）；divisor=4 时主值序列恒为 1 → 步长一变整行被铺成
  全 1/全 0 = 用户实测症状。

## 做了什么

- `js/editor/resize.js`（#91 实现，文件头注释同步重写）：
  - 删除旧 `extractMainValues / detectPeriod / extendValue` 全量重建。
  - 新增「主步块」语义：`cellBlocks`（按每信号 divisor 切完整主步块）、
    `sameBlock` / `detectBlockPeriod`（整块最小周期）、`resampleBlock`（主步内
    分数时间中点重采样 dOld→dNew：首格恒取旧主值，子格按新格中点映射旧格）、
    `extendBlockAt`（缺块延拓：isClockPattern 按块序列最小周期 `blocks[k%p]` 延续，
    不成周期兜底重复末块微形态；其它行延续最后值/空行 -1）。
  - 只改步数（dOld===dNew）：重叠区主步块**原样保留**，不重写用户已画子格图案；
    截断=保留前 newSteps 块；延拓见上。改子步数：重叠主步统一重采样，绝不把
    0101 抹成同值。私有 subSteps 行按 `wpf.divisorOf`（own+1）换算，全局子步变化
    时 dOld===dNew 恒成立 → 只走保留/延拓，不被整体抹平。
  - 保留 `syncSignalMeta / pushUndoSnapshot / scheduleRedraw / remapAnchors`。
- `tools/e2e-ui.mjs` 新增 H 段（真实 DOM `#sample-spin/#substep-spin` change 通道，
  与 D/D2 同通道；建 hclk stride3 自动时钟/hdata Vector 阶梯/hpriv 私有 subSteps=3
  时钟三行，只对本段新行断言）：
  - H1 步数 8→10：hclk 前 24 格原样 + 尾 6 格按 `1,0,1,0` 延续（mains 1/0 交替 10
    步）；hdata 尾 6 格全 7（数据只延续最后值）；hpriv 前 32 格原样 + 尾 8 格继续
    `1,0,1,0,1,0,1,0`；
  - H2 10→8 截断：三行与各自原始快照全等；
  - H3 子步 2→0：hclk 8 格 main 仍 1/0 交替、v 同时含 0 和 1（不塌）；hpriv 仍 32
    格原样（私有行不被全局子步重采样）；
  - H4/H5 子步 0→1 / 1→2 往返：长度 16/24、mains 相位仍 1/0 交替。

## 验证结果

- `node --check js/editor/resize.js` + `node --check tools/e2e-ui.mjs` 通过。
- `node tools/e2e-ui.mjs`：**73/73 全绿**（60 基线 + H 段 13 项；真实 Edge headless，
  零 404、零控制台异常、`__errCount` 为 0）。
- `node tools/regression.mjs`：58/58 全绿。
- ⚠ exe 尚未重建：等 #89 完成后统一 C1 重建 + C8 特征串核验。

## 调试过程（H1 首跑失败归因，防后人踩坑）

- 首跑 72/73：仅 H1「hpriv 尾 8 格」断言失败。实现输出 `1,0,1,0,1,0,1,0`，测试期
  望 `1,0,1,0,0,1,0,1`。
- 归因：hpriv fill 为逐格 `i%2==0?1:0` 且 divisor=4（块长=时钟周期 2 的整数倍）→
  8 个块**两两相同** `[1,0,1,0]`，块序列最小周期 p=1，延拓正确重复块 0。测试误以
  为 stride=4 时块会 `[1,0,1,0]/[0,1,0,1]` 交替（交替只发生在块长为奇数的 stride 3，
  已在 hclk 的 mains 断言覆盖）。**产品代码正确，测试期望写错** → 期望修为
  `1,0,1,0,1,0,1,0` 后全绿。
- 教训：写「块周期延续」类断言前先按 块长 ÷ 底层周期 是否整除 推导块序列是否同相，
  不要凭感觉假设交替。

## 遗留问题 / 下一步

- 下一项 Task2（#89）：信号名标记多比特信号位宽（仅显示层拼 `[msb:lsb]` 局部显示
  名，不改 `sig.name`；位置 wavepaint.clean.js `calculateDynamicNameWidth` /
  `drawSignalName`，见 04 §4.6 Task2 线索）。
- #89 完成后统一 C1 重建 exe + C8 特征串核验 + 全量工具链 + 交付提醒；之后回 Verdi
  先行批 #85 → #86 → #84。

## 交付提醒

- Task4 已按 C2 单独 commit + push 到 `main`（建议消息含 `fix(#91)+docs`）。
- exe 未重建属预期（等 #89 完成后本批收尾统一重建），最终交付前必须重建 + 版本号自查。
