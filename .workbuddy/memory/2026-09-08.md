# 2026-09-08 WavePaint 工作日志

## 本轮目标（用户指令）
1. 调研 Verdi 配套仿真器（VCS 等能否用于本项目）→ 结论：不可用，详见 PROJECT-MASTER.md §7。
2. 定位「parameter 运算问题」是 iverilog 还是我们的代码 → **我们的求值器局限**（探针实测：
   简单参数/乘法可算，嵌套参数/$clog2/移位/sized 字面量全回退 1 位；iverilog 本身全支持）。
3. 生成**跨 AI 跨对话总纲文档**（汇总全部 AI 文档与开发计划：介绍/思路/约束+做法/需求台账/
   路线图/注意事项）→ `PROJECT-MASTER.md`，并确立「改代码必须同步更新该文档」铁律（C17）。
4. 补充要求：借鉴开源项目（Verible/TerosHDL/mshr-h/Surfer/GTKWave/WaveTrace）→ 已纳入总纲 §6.2。
5. 用户指示：parameter 修复的收尾（2 条 regression 断言）**暂不解决，写入交接文档交给其他 AI**。

## parameter 修复——已完成部分（未提交状态由本 commit 保存，收尾见总纲 §6.1）
- 定位探针：`.e2e-tmp/probe-param.mjs` → 升级为长期工具 `tools/probe-param.mjs`
  （真 iverilog 编译+运行参数化设计，三用例：嵌套参数/$clog2/sized 字面量，**全过**）。
- `js/sim/engine.js`：
  - tryEvalExpr 重写：递归参数展开（8 层防循环）、$clog2、`<< >>`、sized 字面量循环归约
    （字面量常在参数值里，代入后才现身，必须放展开循环内）
  - resolvePortWidths：求不出不再清空表达式 → 保留原始 msb/lsb + parametric 标记
  - buildAutoTestbench：**内嵌 parameter 定义进 TB** + 参数化端口拼接原始表达式
    （rangeResolvable：标识符全部有定义才拼，否则回退标量——位宽算术最终由 iverilog 裁决）
  - fmtWidth = max(端口宽, 绑定信号宽)：参数化端口激励不再被 1 位格式化截断
  - widthCompatible：parametric 端口不设限（否则 [W-1:0] 端口永远绑不上矢量信号）
- `tools/regression.mjs`：+4 组用例（求值器升级 6 断言 / TB 内嵌参数 / 参数化端口绑定+回退标量）
- ⚠ 回归现状：红 2 条，**均为断言过时/笔误**（① 旧用例 !/WIDTH/ 与「参数定义合法内嵌 TB」冲突；
  ② 新用例正则把 `reg [9:0]` 写成 `[9:0] reg`），功能本身 e2e-sim 0 失败 + 真 iverilog 全过。
  精确修法已写入总纲 §6.1，交下一个 AI。

## 文档体系变更
- **新建 `.workbuddy/memory/PROJECT-MASTER.md`（项目总纲，跨 AI 单一真相源）**：
  §1 介绍 / §2 思路演进史 / §3 架构与数据模型口径 / §4 约束 C1~C18+对应做法 /
  §5 需求台账（历史摘要+#59~#79）/ §6 进行中交接 + Verdi 路线 P0~P3 /
  §7 仿真器与工具链选型 / §8 坑速查 / §9 跨 AI 工作流 SOP。
- MEMORY.md 头部加总纲指针；REQUIREMENTS.md 加 G 组（#73~#79）。
- 需求图例新增 🔧进行中。

## 提交与交付
- 本轮 commit：parameter 修复 WIP + 总纲文档 + 记忆同步（单一 commit，message 注明交接）。
- exe：engine.js 已改 → 按 C1 已重建（版本号自动带新构建时间与 git 短哈希）。
- ⚠ 交付提醒：重启应用、从唯一路径 `D:\Files\Code\波形\WavePaintClean.exe` 启动、
  面板版本号自查。
