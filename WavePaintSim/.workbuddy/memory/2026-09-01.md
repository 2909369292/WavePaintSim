# 2026-09-01 工作日志

## 第九轮：撤销菜单注入 + 原型链补丁（跨零点，接 08-31 第八轮）

用户对注入方案的实测反馈：分隔线太粗风格不符、菜单已开时再右键注入部分闪烁、
**且核心右键菜单本来就自带 10/16 进制转换项**（重要发现！）→ 注入是重复冲突。

### 决策（提交 `9f3ca5f`，exe 22304256 字节，00:16）
- **撤销全部注入代码**（injectRadixItems 等 0 残留）。进制切换直接用**核心自带的右键项**；
  我们只负责结果正确性 = `patchCoreValueToLabel` + `refreshBusLabels`（按信号自身 radix）。
- `patchCoreValueToLabel` 从「实例属性」扩展到「整条原型链」
  （`Object.getPrototypeOf(dw)` 逐级向上），覆盖核心经内部引用调用的场景。

### DEC/HEX 显示相同位串的症状分析
- 该症状（bin "正常"、dec/hex 都是位串）精确指向：显示标签 = 原始位串 =
  核心**原始** valueToLabel 输出被剥前缀。两个可能：
  1. 核心经内部捕获引用调用，实例补丁够不着 → 本轮已用原型链补丁加覆盖；
  2. 用户测的是 23:45 旧 exe（00:02 构建因文件被占用失败，用户关程序后才成功）。
- 若新 exe 下用核心自带进制项 + 值 ≥10（255→FF）仍不对 → 补丁不可达，
  唯一出路是渲染层重构（用户已表达开放态度，见下）。

### ★ 用户提出：研究重构/重写前端
用户原话：最关心「前端页面以及功能的逻辑」，若混淆无法解析可研究重构重写。
可行性评估（已在回复中给出）：
- 可读资产：index.html / css / js/model.js / utils.js / 全部 feature-*.js / sim-bridge.js
  —— 数据模型（m_signals/values/stride/radix）与业务逻辑基本都在我们这边。
- 混淆的只有 wavepaint.63e6dade.js（画布渲染+交互+菜单），约需重写 1.5k-2.5k 行。
- 分阶段路线：①静态渲染（网格/信号/总线/标签）②鼠标编辑（画/擦/选/粒度）
  ③右键菜单与工具栏 ④切换切换。每阶段可独立验证，可与旧核心并存过渡。

### 构建注意（又一次踩到）
- WavePaint.exe 被运行中的程序锁住 → 编译失败（error CS0016）。
  **构建前先 `tasklist | grep -i wavepaint` 检查**；本次经用户同意 `taskkill /F /IM` 后重建。
  （Git Bash 里 taskkill 参数用 `/F /IM`，`//IM` 会报无效参数。）

### 待用户验收
- 开 00:16 新 exe；右键总线名 → 用**核心自带**的进制项切换
- 用值 255 验证：dec=255 / hex=FF / bin=11111111
- 分隔线/闪烁问题随注入撤销自动消失

---

## 第十轮：0x 前缀统一 + D 异常排查（已提交 `0961950`，exe 01:30 重建）

### 0x 前缀（用户实测：SIM 默认标签带 0x，切 hex 后不带 → 不一致）
- 根源：sim 结果标签用 model.js `formatVectorValue`，hex 分支自带 `'0x'` 前缀。
- 修复：sim.js 新增导出 `formatBusLabel`（剥前缀），sim-bridge.js 本地 `busLabel` 同口径，
  替换 4 处（toNativeSignal / readWaveDocument / summarizeOutputs / vcdToProjectOutputs）。
- 现在 SIM 默认与切 hex 后一致：都无前缀。

### D 显示为虚线（用户怀疑特殊字符）
- **端到端实证**：临时脚本跑真实 iverilog 计数器到 F，
  q=1101 的格子 value=1101、label="D"、hatched=false，VCD→回填→画布全链路无 x。
  → 数据管线无 D 特殊问题。
- 结论：核心把该段渲染为虚线 = 未知(x)，几乎可断定**用户那次仿真在该步输出 x**
  （RTL/激励所致），不是显示 bug。已请用户悬停/点击确认该段取值。
- 教训：本次用「临时 Node 脚本 + 真实 iverilog」端到端复现，比猜核心代码高效得多；
  该手法已沉淀为可复用流程（tools/e2e-sim.mjs 的兄弟做法）。

### 新发现：docs/功能需求_框选与总线编辑_v0.3.0.md（Copilot 写的需求书）
- 明确用户期望：框选编辑应在**编辑（画笔）工具**下直接进行（不是选择工具+浮动条），
  总线框选→输入值、位信号框选→输入 1/0/x/z，粒度跟随顶部整步/子步开关，可撤销。
- 这是下一轮 7.1/7.7/7.8 的**重做规格**，已收编进仓库。

---

## 第十一轮：clock 子步翻转（已提交 `a0fb6fb`，exe 01:50 重建）+ D 排查进展

### clock 子步翻转 ✅
- addPortSignalsToCanvas：kind==='clock' 自动预填 → 每格 1,0,1,0...（从 1 开始）。
- readWaveDocument：仅对 isClockPattern/kind=clock 且每格交替的时钟重建为主步 1,0,1,0...
  （否则主值采样恒为 1 → 时钟沿丢 → 仿真退化）。
- 已验证：clk=10101010、交替数据信号不被误重建（第五轮教训守住了）、slow 保持原采样。

### D（13 渲染成虚线）排查进展 —— 已缩小但未闭环
- 数据层端到端正确（value=1101, label=D, hatched=false）。
- 排除：WaveDrom 映射（本仓库 wavedrom.min.js 里 d 已映射为 "v" 数据，不是 d 边沿）；
  核心 `_0x55dcea` 表（那是画笔状态选择 setCurrentBitState，不是渲染映射，改它会破坏
  d/u 位状态绘制）。
- 每格 wave 字符来自 `sig.waveDromWave[]`，其生成逻辑仍在混淆里。
- **下一步唯一高效手段：请用户打开 视图→WaveDrom→调试面板/显示代码，
  读出 q 信号 wave 串里 D 位置的那个字符**（d/x/z/./=？），即可精准定位修复。

---

## 第十二轮：clock 仿真跟随子步 + D 渲染修复 + v0.3.0 值输入编辑（全部完成并构建）

### 1. Clock 仿真跟随子步（提交 c8119fa）
- 每格交替时钟现在以分数时间驱动 TB（#0.5），仿真频率与画布显示一致。
- readWaveDocument 把每格序列放 clockCells；buildAutoTestbench 用它生成 #(1/stride) 事件。
- 真实 iverilog 验证：q = 0000,0000,0000,0001,0010,0011,0100,0101（每主步+1）。

### 2. D(13) 虚线根因与修复（提交 8da6435）
- probe-core 定位：核心渲染器按 labels[i].toLowerCase() 判定电平字符 l/h/u/d/z，
  总线值 13 标签 "D" 撞上 'd'（下降沿）→ 虚线。只有 13 会撞。
- 修复：给 l/h/u/d/z 判定加 typeof value==='number' 守卫（位信号值数字/总线值字符串）。
  直接改混淆核心（git 可回退），已验证语法。

### 3. v0.3.0 值输入编辑（提交 ccbd5f5）
- 新模块 js/feature-value-edit.js：R1 Bit 弹窗 / 拦截核心 Vector 弹窗、R2 粒度统一
  （indicesByGranularity + 全部走 writeValue）、R3 复用 #wp-modal、R4 撤销、R5 Ctrl 框选。
- feature-select.js applyValues/applyCustom 按粒度收敛；feature-draw Ctrl 让位。
- regression 34/34、e2e 0 失败、exe 22324224 字节。

### 提交信息怪癖
- 本会话 git commit 多次出现"nothing added / pathspec did not match"等误报，
  但提交实际成功（如 ccbd5f5）。判定以 git log 为准，别信命令 stdout。
- 多行提交信息用 `git commit -F <仓库内文件>`（bash 会拆 -m 多行消息）。

### 待用户浏览器验收（无法自动化）
- v0.3.0 验收清单（docs/功能需求_框选与总线编辑_v0.3.0.md §5.1）：R1-R8
- D 现在应显示六边形 "D"；clock 计数速率应与子步显示一致
- R6 实时预览未实现（P1，标注待补）

---

## 第十二轮：clock 仿真跟随子步 + D 渲染修复 + v0.3.0 值输入编辑（全部完成并构建）

### 1. Clock 仿真跟随子步（提交 c8119fa）
- 每格交替时钟现在以分数时间驱动 TB（#0.5），仿真频率与画布显示一致。
- readWaveDocument 把每格序列放 clockCells；buildAutoTestbench 用它生成 #(1/stride) 事件。
- 真实 iverilog 验证：q = 0000,0000,0000,0001,0010,0011,0100,0101（每主步+1）。

### 2. D(13) 虚线根因与修复（提交 8da6435）
- probe-core 定位：核心渲染器按 labels[i].toLowerCase() 判定电平字符 l/h/u/d/z，
  总线值 13 标签 "D" 撞上 'd'（下降沿）→ 虚线。只有 13 会撞。
- 修复：给 l/h/u/d/z 判定加 typeof value==='number' 守卫（位信号值数字/总线值字符串）。
  直接改混淆核心（git 可回退），已验证语法。

### 3. v0.3.0 值输入编辑（提交 ccbd5f5）
- 新模块 js/feature-value-edit.js：R1 Bit 弹窗 / 拦截核心 Vector 弹窗、R2 粒度统一
  （indicesByGranularity + 全部走 writeValue）、R3 复用 #wp-modal、R4 撤销、R5 Ctrl 框选。
- feature-select.js applyValues/applyCustom 按粒度收敛；feature-draw Ctrl 让位。
- regression 34/34、e2e 0 失败、exe 22324224 字节。

### 提交信息怪癖
- 本会话 git commit 多次出现"nothing added / pathspec did not match"等误报，
  但提交实际成功（如 ccbd5f5）。判定以 git log 为准，别信命令 stdout。
- 多行提交信息用 `git commit -F <仓库内文件>`（bash 会拆 -m 多行消息）。

### 待用户浏览器验收（无法自动化）
- v0.3.0 验收清单（docs/功能需求_框选与总线编辑_v0.3.0.md §5.1）：R1-R8
- D 现在应显示六边形 "D"；clock 计数速率应与子步显示一致
- R6 实时预览未实现（P1，标注待补）

---

## 第十三轮：拖动漏格修复 + 框选视觉复刻 + 第二次仿真调查（提交 cc59735，exe 09:35 重建）

用户反馈 4 问题，处理：

### 1. 拖动拉高变时钟（一半子步被拉高）✅ 已修
- 根因：paintMove 只写「本次命中格」，mousemove 稀疏时跳过中间格
  （子步粒度 = 1,0,1,0 时钟样）。
- 修复：feature-draw 新增 paintSpan，拖动时把 last→current 区间全补齐
  （整步按主步首格步进，writeValue 铺满）。真实浏览器验证连续无漏格。

### 2. 单比特框选无视觉 / 多比特选择视觉消失 ✅ 已修
- 根因：feature-value-edit 拦截 mousedown → 核心 range selection 高亮从未出现。
- 修复：feature-common 新增共享高亮层 showRangeHighlight/clearRangeHighlight，
  **复刻核心 drawRangeSelection 视觉**（probe-core #4417-4418 定位：
  rgba(81,1,255,0.2) 紫填充 + 描边、按格子/整行对齐、含 dpr 缩放）。
  feature-value-edit 的 Ctrl/⌘框选、Vector 点击、Bit 点击弹窗前均绘制。

### 3. 第二次仿真出错 ⚠ 调查结论：无法复现失败
- C# 服务端日志（%TEMP%\WavePaintSim_sim.log）每次 compile 0/vvp 0。
- **tools/dev-server.mjs 新增 /api/sim 端点**（Node 版 iverilog/vvp，协议同 exe），
  配合本机 Edge headless + 手写 CDP 脚本（tools/ 临时脚本）做真实浏览器验证：
  连续 3 次仿真全部成功、结果一致、控制台无异常。
- 结论：服务端与前端链路均无「第二次失败」；最可能是激励未绘制（en 全 x）
  → q 恒 0 的提示被误读为出错，或旧 exe。漏格修复后此类现象应大幅减少。

### 4. 复用工具沉淀
- **dev-server.mjs 现在支持 /api/sim**：无需构建 exe 即可浏览器端到端调试仿真。
- **真实浏览器验证法**：Edge headless --remote-debugging-port + 手写 CDP WebSocket
  脚本（Input.dispatchMouseEvent 需带 modifiers:2 才有 Ctrl 效果）。
- git 提交「nothing added」误报再确认：提交实际成功（cc59735），
  以 git log / git show --stat 为准。
- 注意：本仓库 git 根在 D:/Files/Code/波形/（上层），提交内路径带 WavePaintSim/ 前缀。

---

## 第十三轮：拖动漏格修复 + 框选视觉复刻 + 第二次仿真调查（提交 cc59735，exe 09:35 重建）

用户反馈 4 问题，处理：

### 1. 拖动拉高变时钟（一半子步被拉高）✅ 已修
- 根因：paintMove 只写「本次命中格」，mousemove 稀疏时跳过中间格
  （子步粒度 = 1,0,1,0 时钟样）。
- 修复：feature-draw 新增 paintSpan，拖动时把 last→current 区间全补齐
  （整步按主步首格步进，writeValue 铺满）。真实浏览器验证连续无漏格。

### 2. 单比特框选无视觉 / 多比特选择视觉消失 ✅ 已修
- 根因：feature-value-edit 拦截 mousedown → 核心 range selection 高亮从未出现。
- 修复：feature-common 新增共享高亮层 showRangeHighlight/clearRangeHighlight，
  **复刻核心 drawRangeSelection 视觉**（probe-core #4417-4418 定位：
  rgba(81,1,255,0.2) 紫填充 + 描边、按格子/整行对齐、含 dpr 缩放）。
  feature-value-edit 的 Ctrl/⌘框选、Vector 点击、Bit 点击弹窗前均绘制。

### 3. 第二次仿真出错 ⚠ 调查结论：无法复现失败
- C# 服务端日志（%TEMP%\WavePaintSim_sim.log）每次 compile 0/vvp 0。
- **tools/dev-server.mjs 新增 /api/sim 端点**（Node 版 iverilog/vvp，协议同 exe），
  配合本机 Edge headless + 手写 CDP 脚本（tools/ 临时脚本）做真实浏览器验证：
  连续 3 次仿真全部成功、结果一致、控制台无异常。
- 结论：服务端与前端链路均无「第二次失败」；最可能是激励未绘制（en 全 x）
  → q 恒 0 的提示被误读为出错，或旧 exe。漏格修复后此类现象应大幅减少。

### 4. 复用工具沉淀
- **dev-server.mjs 现在支持 /api/sim**：无需构建 exe 即可浏览器端到端调试仿真。
- **真实浏览器验证法**：Edge headless --remote-debugging-port + 手写 CDP WebSocket
  脚本（Input.dispatchMouseEvent 需带 modifiers:2 才有 Ctrl 效果）。
- git 提交「nothing added」误报再确认：提交实际成功（cc59735），
  以 git log / git show --stat 为准。
- 注意：本仓库 git 根在 D:/Files/Code/波形/（上层），提交内路径带 WavePaintSim/ 前缀。
