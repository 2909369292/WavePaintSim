# 2026-09-01 工作日志

## 第九轮：撤销菜单注入 + 原型链补丁（跨零点，接 08-31 第八轮）

用户对注入方案的实测反馈：分隔线太粗风格不符、菜单已开时再右键注入部分闪烁、
**且核心右键菜单本来就自带 10/16 进制转换项**（重要发现！）→ 注入是重复冲突。

### 决策（提交 `9f3ca5f`，exe 22304256 字节，00:16）
- **撤销全部注入代码**（injectRadixItems 等 0 残留）。进制切换直接用**核心自带的右键项**；
  我们只负责结果正确性 = `patchCoreValueToLabel` + `refreshBusLabels`（按信号自身 radix）。
- `patchCoreValueToLabel` 从「实例属性」扩展到「整条原型链」
  （`Object.getPrototypeOf(dw)` 逐级向上），覆盖核心经内部引用调用的场景。

### DEC/HEX 显示相同位串的症状分析
- 该症状（bin "正常"、dec/hex 都是位串）精确指向：显示标签 = 原始位串 =
  核心**原始** valueToLabel 输出被剥前缀。两个可能：
  1. 核心经内部捕获引用调用，实例补丁够不着 → 本轮已用原型链补丁加覆盖；
  2. 用户测的是 23:45 旧 exe（00:02 构建因文件被占用失败，用户关程序后才成功）。
- 若新 exe 下用核心自带进制项 + 值 ≥10（255→FF）仍不对 → 补丁不可达，
  唯一出路是渲染层重构（用户已表达开放态度，见下）。

### ★ 用户提出：研究重构/重写前端
用户原话：最关心「前端页面以及功能的逻辑」，若混淆无法解析可研究重构重写。
可行性评估（已在回复中给出）：
- 可读资产：index.html / css / js/model.js / utils.js / 全部 feature-*.js / sim-bridge.js
  —— 数据模型（m_signals/values/stride/radix）与业务逻辑基本都在我们这边。
- 混淆的只有 wavepaint.63e6dade.js（画布渲染+交互+菜单），约需重写 1.5k-2.5k 行。
- 分阶段路线：①静态渲染（网格/信号/总线/标签）②鼠标编辑（画/擦/选/粒度）
  ③右键菜单与工具栏 ④切换切换。每阶段可独立验证，可与旧核心并存过渡。

### 构建注意（又一次踩到）
- WavePaint.exe 被运行中的程序锁住 → 编译失败（error CS0016）。
  **构建前先 `tasklist | grep -i wavepaint` 检查**；本次经用户同意 `taskkill /F /IM` 后重建。
  （Git Bash 里 taskkill 参数用 `/F /IM`，`//IM` 会报无效参数。）

### 待用户验收
- 开 00:16 新 exe；右键总线名 → 用**核心自带**的进制项切换
- 用值 255 验证：dec=255 / hex=FF / bin=11111111
- 分隔线/闪烁问题随注入撤销自动消失

---

## 第十轮：0x 前缀统一 + D 异常排查（已提交 `0961950`，exe 01:30 重建）

### 0x 前缀（用户实测：SIM 默认标签带 0x，切 hex 后不带 → 不一致）
- 根源：sim 结果标签用 model.js `formatVectorValue`，hex 分支自带 `'0x'` 前缀。
- 修复：sim.js 新增导出 `formatBusLabel`（剥前缀），sim-bridge.js 本地 `busLabel` 同口径，
  替换 4 处（toNativeSignal / readWaveDocument / summarizeOutputs / vcdToProjectOutputs）。
- 现在 SIM 默认与切 hex 后一致：都无前缀。

### D 显示为虚线（用户怀疑特殊字符）
- **端到端实证**：临时脚本跑真实 iverilog 计数器到 F，
  q=1101 的格子 value=1101、label="D"、hatched=false，VCD→回填→画布全链路无 x。
  → 数据管线无 D 特殊问题。
- 结论：核心把该段渲染为虚线 = 未知(x)，几乎可断定**用户那次仿真在该步输出 x**
  （RTL/激励所致），不是显示 bug。已请用户悬停/点击确认该段取值。
- 教训：本次用「临时 Node 脚本 + 真实 iverilog」端到端复现，比猜核心代码高效得多；
  该手法已沉淀为可复用流程（tools/e2e-sim.mjs 的兄弟做法）。

### 新发现：docs/功能需求_框选与总线编辑_v0.3.0.md（Copilot 写的需求书）
- 明确用户期望：框选编辑应在**编辑（画笔）工具**下直接进行（不是选择工具+浮动条），
  总线框选→输入值、位信号框选→输入 1/0/x/z，粒度跟随顶部整步/子步开关，可撤销。
- 这是下一轮 7.1/7.7/7.8 的**重做规格**，已收编进仓库。
